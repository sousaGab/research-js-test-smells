file,type,line,method,methodStart,methodEnd,source
/src/__tests__/testUtils-test.js,IdenticalTestDescription,"{'startLine':91,'endLine':97}","it('should apply transformation', async () => {
      const moduleToTransform = getModuleToTransform();
      const transformedCode = await testUtils.applyTransform(testAsyncTransform, null, moduleToTransform);

      expect(transformedCode).not.toMatch(/sum/);
      expect(transformedCode).toMatch(/addition/);
    })",91,97,snuts
/src/__tests__/testUtils-test.js,IdenticalTestDescription,"{'startLine':99,'endLine':101}","it('should run test', () => {
      return testUtils.runTest(__dirname, path.join('__testfixtures__', 'test-async-transform'), null, 'test-async-transform');
    })",99,101,snuts
/src/__tests__/testUtils-test.js,IdenticalTestDescription,"{'startLine':103,'endLine':106}","it ('should run snapshot test', () => {
      const moduleToTransform = getModuleToTransform();
      return testUtils.runSnapshotTest(testAsyncTransform, null, moduleToTransform);
    })",103,106,snuts
/src/__tests__/testUtils-test.js,IdenticalTestDescription,"{'startLine':108,'endLine':111}","it('should run inline test', () => {
      const moduleToTransform = getModuleToTransform();
      return testUtils.runInlineTest(testAsyncTransform, null, moduleToTransform, expectedInlineOutput);
    })",108,111,snuts
/src/__tests__/matchNode-test.js,AnonymousTest,"{'startLine':33,'endLine':45}","it('matches scalars', function() {
    expect('foo').toMatchNode('foo');
    expect('foo').not.toMatchNode('bar');
    expect('123').not.toMatchNode(123);

    expect(123).toMatchNode(123);
    expect(123).not.toMatchNode(456);
    expect(123).not.toMatchNode('123');

    expect(true).toMatchNode(true);
    expect(true).not.toMatchNode(false);
    expect(true).not.toMatchNode('true');
  })",33,45,snuts
/src/__tests__/matchNode-test.js,AnonymousTest,"{'startLine':47,'endLine':57}","it('matches arrays', function() {
    expect([1, 2, 3]).toMatchNode([1, 2, 3]);
    expect([1, 2, 3]).not.toMatchNode([4, 5, 6]);

    expect([[1, 2, 3], 'foo']).toMatchNode([[1, 2, 3], 'foo']);
    expect([[1, 2, 3], 'foo']).not.toMatchNode([[456], 'foo']);
    expect([[1, 2, 3], 'foo']).not.toMatchNode([[1, 2, 3], 'bar']);

    expect([1, 2, 3, 4]).toMatchNode([1, 2, 3]);
    expect([1, 2, 3]).not.toMatchNode([1, 2, 3, 4]);
  })",47,57,snuts
/src/__tests__/matchNode-test.js,AnonymousTest,"{'startLine':59,'endLine':74}","it('matches objects', function() {
    expect({}).toMatchNode({});
    expect({name: 'foo'}).toMatchNode({name: 'foo'});
    expect({name: 'foo'}).not.toMatchNode({name: 'bar'});

    expect({name: 'foo', value: {name: 'bar'}})
      .toMatchNode({name: 'foo', value: {name: 'bar'}});
    expect({name: 'foo', value: {name: 'bar'}})
      .not.toMatchNode({name: 'foo', value: {name: 'baz'}});

    expect({name: 'foo', value: 'bar'}).toMatchNode({name: 'foo'});
    expect({name: 'foo'}).not.toMatchNode({name: 'foo', value: 'bar'});

    expect(Object.create({name: 'foo'})).not.toMatchNode({name: 'foo'});
    expect({}).toMatchNode(Object.create({name: 'foo'}));
  })",59,74,snuts
/src/__tests__/matchNode-test.js,SubOptimalAssert,"{'startLine':27,'endLine':27}","it('matches null and undefined', function() {
    expect(null).toMatchNode(null);
    expect(null).not.toMatchNode(undefined);

    expect(undefined).toMatchNode(undefined);
    expect(undefined).not.toMatchNode(null);
  })",25,31,snuts
/src/__tests__/matchNode-test.js,SubOptimalAssert,"{'startLine':30,'endLine':30}","it('matches null and undefined', function() {
    expect(null).toMatchNode(null);
    expect(null).not.toMatchNode(undefined);

    expect(undefined).toMatchNode(undefined);
    expect(undefined).not.toMatchNode(null);
  })",25,31,snuts
/src/__tests__/argsParser-test.js,AnonymousTest,"{'startLine':363,'endLine':372}","it('understands --no-prefixes', function() {
      expect(parser.parse(['--no-foo', '--no-another-bar']))
        .toEqual({
          options: {
            foo: false,
            bar: false,
          },
          positionalArguments: [],
        });
    })",363,372,snuts
/src/__tests__/Worker-test.js,AnonymousTest,"{'startLine':18,'endLine':32}","it('transforms files', done => {
    const worker = require('../Worker');
    const transformPath =
      createTransformWith('return fileInfo.source + "" changed"";');
    const sourcePath = createTempFileWith('foo');
    const emitter = worker([transformPath]);

    emitter.send({files: [sourcePath]});
    emitter.once('message', (data) => {
      expect(data.status).toBe('ok');
      expect(data.msg).toBe(sourcePath);
      expect(getFileContent(sourcePath)).toBe('foo changed');
      done();
    });
  })",18,32,snuts
/src/__tests__/Collection-test.js,AnonymousTest,"{'startLine':286,'endLine':294}","it('dedupes elements', function() {
        const path = new NodePath(nodes[0]);
        const root = Collection.fromNodes(nodes);
        const mapped = root.map(() => path);

        expect(root).not.toBe(mapped);
        expect(mapped.length).toBe(1);
        expect(mapped.paths()[0]).toBe(path);
      })",286,294,snuts
/src/__tests__/Collection-test.js,GeneralFixture,"{'startLine':24,'endLine':24}","beforeEach(function() {
    jest.resetModules();

    Collection = require('../Collection');
    recast = require('recast');

    NodePath = recast.types.NodePath;
    types = recast.types.namedTypes;
    b = recast.types.builders;

    nodes = [b.identifier('foo'), b.identifier('bar')];
  })",20,31,snuts
/src/__tests__/Collection-test.js,NonFunctionalStatement,"{'startLine':118,'endLine':118}","it('handles type inheritance chains', function() {
      const Collection = require('../Collection');
      const nodeMethod = function() {};
      const identifierMethod = function() {};
      Collection.registerMethods({nodeMethod: nodeMethod}, types.Node);
      Collection.registerMethods(
        {identifierMethod: identifierMethod},
        types.Identifier
      );

      const collection = Collection.fromNodes([b.identifier('foo')]);

      expect(() => collection.identifierMethod()).not.toThrow();
      expect(() => collection.nodeMethod()).not.toThrow();
    })",116,130,snuts
/src/__tests__/Collection-test.js,NonFunctionalStatement,"{'startLine':119,'endLine':119}","it('handles type inheritance chains', function() {
      const Collection = require('../Collection');
      const nodeMethod = function() {};
      const identifierMethod = function() {};
      Collection.registerMethods({nodeMethod: nodeMethod}, types.Node);
      Collection.registerMethods(
        {identifierMethod: identifierMethod},
        types.Identifier
      );

      const collection = Collection.fromNodes([b.identifier('foo')]);

      expect(() => collection.identifierMethod()).not.toThrow();
      expect(() => collection.nodeMethod()).not.toThrow();
    })",116,130,snuts
/src/__tests__/Collection-test.js,NonFunctionalStatement,"{'startLine':134,'endLine':134}","it('handles type inheritance with multiple parents', function() {
      Collection.registerMethods(
        {expressionMethod: function() {}},
        types.Expression
      );
      const collection = Collection.fromNodes([
        b.functionExpression(null, [], b.blockStatement([]))
      ]);
      expect(() => collection.expressionMethod()).not.toThrow();
    })",132,141,snuts
/src/__tests__/Collection-test.js,NonFunctionalStatement,"{'startLine':145,'endLine':145}","it('allows multiple registrations for non-conflicting types', function () {
      Collection.registerMethods(
        {foo: function () {}},
        types.FunctionExpression
      );

      Collection.registerMethods(
        {foo: function () {}},
        types.BinaryExpression
      );

      const collection = Collection.fromNodes([
        b.functionExpression(null, [], b.blockStatement([])),
        b.functionExpression(null, [], b.blockStatement([])),
        b.binaryExpression('+', b.identifier('a'), b.identifier('b'))
      ]);

      function typeFilter(type) {
        return function (path) {
          return type.check(path.value);
        };
      }

      // allowed if collection contents match one of the registered types.
      collection.filter(typeFilter(types.BinaryExpression)).foo();
      collection.filter(typeFilter(types.FunctionExpression)).foo();

      // not allowed if there is mixed types (even though all types match one function or the other).
      expect(function () {
        collection.foo();
      }).toThrow();
    })",143,174,snuts
/src/__tests__/Collection-test.js,NonFunctionalStatement,"{'startLine':150,'endLine':150}","it('allows multiple registrations for non-conflicting types', function () {
      Collection.registerMethods(
        {foo: function () {}},
        types.FunctionExpression
      );

      Collection.registerMethods(
        {foo: function () {}},
        types.BinaryExpression
      );

      const collection = Collection.fromNodes([
        b.functionExpression(null, [], b.blockStatement([])),
        b.functionExpression(null, [], b.blockStatement([])),
        b.binaryExpression('+', b.identifier('a'), b.identifier('b'))
      ]);

      function typeFilter(type) {
        return function (path) {
          return type.check(path.value);
        };
      }

      // allowed if collection contents match one of the registered types.
      collection.filter(typeFilter(types.BinaryExpression)).foo();
      collection.filter(typeFilter(types.FunctionExpression)).foo();

      // not allowed if there is mixed types (even though all types match one function or the other).
      expect(function () {
        collection.foo();
      }).toThrow();
    })",143,174,snuts
/src/__tests__/Collection-test.js,NonFunctionalStatement,"{'startLine':179,'endLine':179}","describe('hasConflictingRegistration', function () {
      function register(methodName, type) {
        const methods = {};
        methods[methodName] = function () {};
        if (!types[type]) {
          throw new Error(type + ' is not a valid type');
        }
        Collection.registerMethods(methods, types[type]);
      }

      it('true if supertype is registered', function () {
        register('supertypeIsRegistered', 'Expression');
        expect(Collection.hasConflictingRegistration('supertypeIsRegistered', 'FunctionExpression')).toBe(true);
      });

      it('true if subtype is registered', function () {
        register('subtypeIsRegistered', 'FunctionExpression');
        expect(Collection.hasConflictingRegistration('subtypeIsRegistered', 'Expression')).toBe(true);
      });

      it('false if only a sibling type is registered', function () {
        register('siblingIsRegistered', 'FunctionExpression');
        expect(Collection.hasConflictingRegistration('siblingIsRegistered', 'BinaryExpression')).toBe(false);
      });
    })",176,200,snuts
/src/__tests__/Collection-test.js,NonFunctionalStatement,"{'startLine':229,'endLine':229}","it('returns the collection itself', function() {
        const fVariables = Collection.fromNodes(nodes);
        const result = fVariables.forEach(function(){});

        expect(result).toBe(fVariables);
      })",227,232,snuts
/src/__tests__/Collection-test.js,SubOptimalAssert,"{'startLine':211,'endLine':211}","it('lets you filter with custom logic', function() {
        const filter = jest.fn(function(path) {
          return path.value.name === 'foo';
        });
        const fooVariables = Collection.fromNodes(nodes).filter(filter);

        expect(filter.mock.calls.length).toBe(2);
        expect(fooVariables.length).toBe(1);
      })",205,213,snuts
/src/__tests__/Collection-test.js,SubOptimalAssert,"{'startLine':212,'endLine':212}","it('lets you filter with custom logic', function() {
        const filter = jest.fn(function(path) {
          return path.value.name === 'foo';
        });
        const fooVariables = Collection.fromNodes(nodes).filter(filter);

        expect(filter.mock.calls.length).toBe(2);
        expect(fooVariables.length).toBe(1);
      })",205,213,snuts
/src/__tests__/Collection-test.js,SubOptimalAssert,"{'startLine':222,'endLine':222}","it('lets you iterate over each element of an collection', function() {
        const each = jest.fn();
        Collection.fromNodes(nodes).forEach(each);

        expect(each.mock.calls.length).toBe(2);
        expect(each.mock.calls[0][0].value).toBe(nodes[0]);
        expect(each.mock.calls[1][0].value).toBe(nodes[1]);
      })",218,225,snuts
/src/__tests__/Collection-test.js,SubOptimalAssert,"{'startLine':240,'endLine':240}","it('lets you test each element of a collection and stops when one passes the test', function() {
        const each = jest.fn().mockImplementation(() => true);
        Collection.fromNodes(nodes).some(each);

        expect(each.mock.calls.length).toBe(1);
        expect(each.mock.calls[0][0].value).toBe(nodes[0]);
      })",236,242,snuts
/src/__tests__/Collection-test.js,SubOptimalAssert,"{'startLine':260,'endLine':260}","it('lets you test each element of a collection and stops when one fails the test', function() {
        const each = jest.fn().mockImplementation(() => false);
        Collection.fromNodes(nodes).every(each);

        expect(each.mock.calls.length).toBe(1);
        expect(each.mock.calls[0][0].value).toBe(nodes[0]);
      })",256,262,snuts
/src/__tests__/Collection-test.js,SubOptimalAssert,"{'startLine':281,'endLine':281}","it('returns a new collection with mapped values', function() {
        const root = Collection.fromNodes(nodes);
        const mapped = root.map((_, i) => new NodePath(nodes[+!i]));

        expect(root).not.toBe(mapped);
        expect(mapped.length).toBe(2);
        expect(mapped.nodes()[0]).toBe(nodes[1]);
        expect(mapped.nodes()[1]).toBe(nodes[0]);
      })",276,284,snuts
/src/__tests__/Collection-test.js,SubOptimalAssert,"{'startLine':292,'endLine':292}","it('dedupes elements', function() {
        const path = new NodePath(nodes[0]);
        const root = Collection.fromNodes(nodes);
        const mapped = root.map(() => path);

        expect(root).not.toBe(mapped);
        expect(mapped.length).toBe(1);
        expect(mapped.paths()[0]).toBe(path);
      })",286,294,snuts
/dist/__tests__/testUtils-test.js,IdenticalTestDescription,"{'startLine':91,'endLine':97}","it('should apply transformation', async () => {
      const moduleToTransform = getModuleToTransform();
      const transformedCode = await testUtils.applyTransform(testAsyncTransform, null, moduleToTransform);

      expect(transformedCode).not.toMatch(/sum/);
      expect(transformedCode).toMatch(/addition/);
    })",91,97,snuts
/dist/__tests__/testUtils-test.js,IdenticalTestDescription,"{'startLine':99,'endLine':101}","it('should run test', () => {
      return testUtils.runTest(__dirname, path.join('__testfixtures__', 'test-async-transform'), null, 'test-async-transform');
    })",99,101,snuts
/dist/__tests__/testUtils-test.js,IdenticalTestDescription,"{'startLine':103,'endLine':106}","it ('should run snapshot test', () => {
      const moduleToTransform = getModuleToTransform();
      return testUtils.runSnapshotTest(testAsyncTransform, null, moduleToTransform);
    })",103,106,snuts
/dist/__tests__/testUtils-test.js,IdenticalTestDescription,"{'startLine':108,'endLine':111}","it('should run inline test', () => {
      const moduleToTransform = getModuleToTransform();
      return testUtils.runInlineTest(testAsyncTransform, null, moduleToTransform, expectedInlineOutput);
    })",108,111,snuts
/dist/__tests__/matchNode-test.js,AnonymousTest,"{'startLine':33,'endLine':45}","it('matches scalars', function() {
    expect('foo').toMatchNode('foo');
    expect('foo').not.toMatchNode('bar');
    expect('123').not.toMatchNode(123);

    expect(123).toMatchNode(123);
    expect(123).not.toMatchNode(456);
    expect(123).not.toMatchNode('123');

    expect(true).toMatchNode(true);
    expect(true).not.toMatchNode(false);
    expect(true).not.toMatchNode('true');
  })",33,45,snuts
/dist/__tests__/matchNode-test.js,AnonymousTest,"{'startLine':47,'endLine':57}","it('matches arrays', function() {
    expect([1, 2, 3]).toMatchNode([1, 2, 3]);
    expect([1, 2, 3]).not.toMatchNode([4, 5, 6]);

    expect([[1, 2, 3], 'foo']).toMatchNode([[1, 2, 3], 'foo']);
    expect([[1, 2, 3], 'foo']).not.toMatchNode([[456], 'foo']);
    expect([[1, 2, 3], 'foo']).not.toMatchNode([[1, 2, 3], 'bar']);

    expect([1, 2, 3, 4]).toMatchNode([1, 2, 3]);
    expect([1, 2, 3]).not.toMatchNode([1, 2, 3, 4]);
  })",47,57,snuts
/dist/__tests__/matchNode-test.js,AnonymousTest,"{'startLine':59,'endLine':74}","it('matches objects', function() {
    expect({}).toMatchNode({});
    expect({name: 'foo'}).toMatchNode({name: 'foo'});
    expect({name: 'foo'}).not.toMatchNode({name: 'bar'});

    expect({name: 'foo', value: {name: 'bar'}})
      .toMatchNode({name: 'foo', value: {name: 'bar'}});
    expect({name: 'foo', value: {name: 'bar'}})
      .not.toMatchNode({name: 'foo', value: {name: 'baz'}});

    expect({name: 'foo', value: 'bar'}).toMatchNode({name: 'foo'});
    expect({name: 'foo'}).not.toMatchNode({name: 'foo', value: 'bar'});

    expect(Object.create({name: 'foo'})).not.toMatchNode({name: 'foo'});
    expect({}).toMatchNode(Object.create({name: 'foo'}));
  })",59,74,snuts
/dist/__tests__/matchNode-test.js,SubOptimalAssert,"{'startLine':27,'endLine':27}","it('matches null and undefined', function() {
    expect(null).toMatchNode(null);
    expect(null).not.toMatchNode(undefined);

    expect(undefined).toMatchNode(undefined);
    expect(undefined).not.toMatchNode(null);
  })",25,31,snuts
/dist/__tests__/matchNode-test.js,SubOptimalAssert,"{'startLine':30,'endLine':30}","it('matches null and undefined', function() {
    expect(null).toMatchNode(null);
    expect(null).not.toMatchNode(undefined);

    expect(undefined).toMatchNode(undefined);
    expect(undefined).not.toMatchNode(null);
  })",25,31,snuts
/dist/__tests__/argsParser-test.js,AnonymousTest,"{'startLine':363,'endLine':372}","it('understands --no-prefixes', function() {
      expect(parser.parse(['--no-foo', '--no-another-bar']))
        .toEqual({
          options: {
            foo: false,
            bar: false,
          },
          positionalArguments: [],
        });
    })",363,372,snuts
/dist/__tests__/Worker-test.js,AnonymousTest,"{'startLine':18,'endLine':32}","it('transforms files', done => {
    const worker = require('../Worker');
    const transformPath =
      createTransformWith('return fileInfo.source + "" changed"";');
    const sourcePath = createTempFileWith('foo');
    const emitter = worker([transformPath]);

    emitter.send({files: [sourcePath]});
    emitter.once('message', (data) => {
      expect(data.status).toBe('ok');
      expect(data.msg).toBe(sourcePath);
      expect(getFileContent(sourcePath)).toBe('foo changed');
      done();
    });
  })",18,32,snuts
/dist/__tests__/Collection-test.js,AnonymousTest,"{'startLine':286,'endLine':294}","it('dedupes elements', function() {
        const path = new NodePath(nodes[0]);
        const root = Collection.fromNodes(nodes);
        const mapped = root.map(() => path);

        expect(root).not.toBe(mapped);
        expect(mapped.length).toBe(1);
        expect(mapped.paths()[0]).toBe(path);
      })",286,294,snuts
/dist/__tests__/Collection-test.js,GeneralFixture,"{'startLine':24,'endLine':24}","beforeEach(function() {
    jest.resetModules();

    Collection = require('../Collection');
    recast = require('recast');

    NodePath = recast.types.NodePath;
    types = recast.types.namedTypes;
    b = recast.types.builders;

    nodes = [b.identifier('foo'), b.identifier('bar')];
  })",20,31,snuts
/dist/__tests__/Collection-test.js,NonFunctionalStatement,"{'startLine':118,'endLine':118}","it('handles type inheritance chains', function() {
      const Collection = require('../Collection');
      const nodeMethod = function() {};
      const identifierMethod = function() {};
      Collection.registerMethods({nodeMethod: nodeMethod}, types.Node);
      Collection.registerMethods(
        {identifierMethod: identifierMethod},
        types.Identifier
      );

      const collection = Collection.fromNodes([b.identifier('foo')]);

      expect(() => collection.identifierMethod()).not.toThrow();
      expect(() => collection.nodeMethod()).not.toThrow();
    })",116,130,snuts
/dist/__tests__/Collection-test.js,NonFunctionalStatement,"{'startLine':119,'endLine':119}","it('handles type inheritance chains', function() {
      const Collection = require('../Collection');
      const nodeMethod = function() {};
      const identifierMethod = function() {};
      Collection.registerMethods({nodeMethod: nodeMethod}, types.Node);
      Collection.registerMethods(
        {identifierMethod: identifierMethod},
        types.Identifier
      );

      const collection = Collection.fromNodes([b.identifier('foo')]);

      expect(() => collection.identifierMethod()).not.toThrow();
      expect(() => collection.nodeMethod()).not.toThrow();
    })",116,130,snuts
/dist/__tests__/Collection-test.js,NonFunctionalStatement,"{'startLine':134,'endLine':134}","it('handles type inheritance with multiple parents', function() {
      Collection.registerMethods(
        {expressionMethod: function() {}},
        types.Expression
      );
      const collection = Collection.fromNodes([
        b.functionExpression(null, [], b.blockStatement([]))
      ]);
      expect(() => collection.expressionMethod()).not.toThrow();
    })",132,141,snuts
/dist/__tests__/Collection-test.js,NonFunctionalStatement,"{'startLine':145,'endLine':145}","it('allows multiple registrations for non-conflicting types', function () {
      Collection.registerMethods(
        {foo: function () {}},
        types.FunctionExpression
      );

      Collection.registerMethods(
        {foo: function () {}},
        types.BinaryExpression
      );

      const collection = Collection.fromNodes([
        b.functionExpression(null, [], b.blockStatement([])),
        b.functionExpression(null, [], b.blockStatement([])),
        b.binaryExpression('+', b.identifier('a'), b.identifier('b'))
      ]);

      function typeFilter(type) {
        return function (path) {
          return type.check(path.value);
        };
      }

      // allowed if collection contents match one of the registered types.
      collection.filter(typeFilter(types.BinaryExpression)).foo();
      collection.filter(typeFilter(types.FunctionExpression)).foo();

      // not allowed if there is mixed types (even though all types match one function or the other).
      expect(function () {
        collection.foo();
      }).toThrow();
    })",143,174,snuts
/dist/__tests__/Collection-test.js,NonFunctionalStatement,"{'startLine':150,'endLine':150}","it('allows multiple registrations for non-conflicting types', function () {
      Collection.registerMethods(
        {foo: function () {}},
        types.FunctionExpression
      );

      Collection.registerMethods(
        {foo: function () {}},
        types.BinaryExpression
      );

      const collection = Collection.fromNodes([
        b.functionExpression(null, [], b.blockStatement([])),
        b.functionExpression(null, [], b.blockStatement([])),
        b.binaryExpression('+', b.identifier('a'), b.identifier('b'))
      ]);

      function typeFilter(type) {
        return function (path) {
          return type.check(path.value);
        };
      }

      // allowed if collection contents match one of the registered types.
      collection.filter(typeFilter(types.BinaryExpression)).foo();
      collection.filter(typeFilter(types.FunctionExpression)).foo();

      // not allowed if there is mixed types (even though all types match one function or the other).
      expect(function () {
        collection.foo();
      }).toThrow();
    })",143,174,snuts
/dist/__tests__/Collection-test.js,NonFunctionalStatement,"{'startLine':179,'endLine':179}","describe('hasConflictingRegistration', function () {
      function register(methodName, type) {
        const methods = {};
        methods[methodName] = function () {};
        if (!types[type]) {
          throw new Error(type + ' is not a valid type');
        }
        Collection.registerMethods(methods, types[type]);
      }

      it('true if supertype is registered', function () {
        register('supertypeIsRegistered', 'Expression');
        expect(Collection.hasConflictingRegistration('supertypeIsRegistered', 'FunctionExpression')).toBe(true);
      });

      it('true if subtype is registered', function () {
        register('subtypeIsRegistered', 'FunctionExpression');
        expect(Collection.hasConflictingRegistration('subtypeIsRegistered', 'Expression')).toBe(true);
      });

      it('false if only a sibling type is registered', function () {
        register('siblingIsRegistered', 'FunctionExpression');
        expect(Collection.hasConflictingRegistration('siblingIsRegistered', 'BinaryExpression')).toBe(false);
      });
    })",176,200,snuts
/dist/__tests__/Collection-test.js,NonFunctionalStatement,"{'startLine':229,'endLine':229}","it('returns the collection itself', function() {
        const fVariables = Collection.fromNodes(nodes);
        const result = fVariables.forEach(function(){});

        expect(result).toBe(fVariables);
      })",227,232,snuts
/dist/__tests__/Collection-test.js,SubOptimalAssert,"{'startLine':211,'endLine':211}","it('lets you filter with custom logic', function() {
        const filter = jest.fn(function(path) {
          return path.value.name === 'foo';
        });
        const fooVariables = Collection.fromNodes(nodes).filter(filter);

        expect(filter.mock.calls.length).toBe(2);
        expect(fooVariables.length).toBe(1);
      })",205,213,snuts
/dist/__tests__/Collection-test.js,SubOptimalAssert,"{'startLine':212,'endLine':212}","it('lets you filter with custom logic', function() {
        const filter = jest.fn(function(path) {
          return path.value.name === 'foo';
        });
        const fooVariables = Collection.fromNodes(nodes).filter(filter);

        expect(filter.mock.calls.length).toBe(2);
        expect(fooVariables.length).toBe(1);
      })",205,213,snuts
/dist/__tests__/Collection-test.js,SubOptimalAssert,"{'startLine':222,'endLine':222}","it('lets you iterate over each element of an collection', function() {
        const each = jest.fn();
        Collection.fromNodes(nodes).forEach(each);

        expect(each.mock.calls.length).toBe(2);
        expect(each.mock.calls[0][0].value).toBe(nodes[0]);
        expect(each.mock.calls[1][0].value).toBe(nodes[1]);
      })",218,225,snuts
/dist/__tests__/Collection-test.js,SubOptimalAssert,"{'startLine':240,'endLine':240}","it('lets you test each element of a collection and stops when one passes the test', function() {
        const each = jest.fn().mockImplementation(() => true);
        Collection.fromNodes(nodes).some(each);

        expect(each.mock.calls.length).toBe(1);
        expect(each.mock.calls[0][0].value).toBe(nodes[0]);
      })",236,242,snuts
/dist/__tests__/Collection-test.js,SubOptimalAssert,"{'startLine':260,'endLine':260}","it('lets you test each element of a collection and stops when one fails the test', function() {
        const each = jest.fn().mockImplementation(() => false);
        Collection.fromNodes(nodes).every(each);

        expect(each.mock.calls.length).toBe(1);
        expect(each.mock.calls[0][0].value).toBe(nodes[0]);
      })",256,262,snuts
/dist/__tests__/Collection-test.js,SubOptimalAssert,"{'startLine':281,'endLine':281}","it('returns a new collection with mapped values', function() {
        const root = Collection.fromNodes(nodes);
        const mapped = root.map((_, i) => new NodePath(nodes[+!i]));

        expect(root).not.toBe(mapped);
        expect(mapped.length).toBe(2);
        expect(mapped.nodes()[0]).toBe(nodes[1]);
        expect(mapped.nodes()[1]).toBe(nodes[0]);
      })",276,284,snuts
/dist/__tests__/Collection-test.js,SubOptimalAssert,"{'startLine':292,'endLine':292}","it('dedupes elements', function() {
        const path = new NodePath(nodes[0]);
        const root = Collection.fromNodes(nodes);
        const mapped = root.map(() => path);

        expect(root).not.toBe(mapped);
        expect(mapped.length).toBe(1);
        expect(mapped.paths()[0]).toBe(path);
      })",286,294,snuts
/bin/__tests__/jscodeshift-test.js,IdenticalTestDescription,"{'startLine':311,'endLine':323}","it('sources ignore patterns from configuration file', () => {
      const patterns = ['sub/dir/', '*-test.js'];
      const gitignore = createTempFileWith(patterns.join('\n'), '.gitignore');
      sources.push(createTempFileWith('subfile', 'sub/dir/file.js'));

      return run(['-t', transform, '--ignore-config', gitignore].concat(sources)).then(
        () => {
          expect(readFile(sources[0]).toString()).toBe('transforma');
          expect(readFile(sources[1]).toString()).toBe('a');
          expect(readFile(sources[2]).toString()).toBe('subfile');
        }
      );
    })",311,323,snuts
/src/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':68,'endLine':68}","it('finds all variable declarators', function() {
      const declarators = Collection.fromNodes(nodes).findVariableDeclarators();
      expect(declarators.getTypes()).toContain('VariableDeclarator');
      expect(declarators.length).toBe(7);
    })",65,69,snuts
/src/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':74,'endLine':74}","it('finds variable declarators by name', function() {
      const declarators = Collection.fromNodes(nodes)
        .findVariableDeclarators('bar');
      expect(declarators.length).toBe(2);
    })",71,75,snuts
/src/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':84,'endLine':84}","it('finds module imports (require)', function() {
      const declarators = Collection.fromNodes(nodes)
        .findVariableDeclarators()
        .filter(VariableDeclaratorCollection.filters.requiresModule());

      expect(declarators.length).toBe(2);
    })",79,85,snuts
/src/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':92,'endLine':92}","it('finds module imports (require) by module name', function() {
      const declarators = Collection.fromNodes(nodes)
        .findVariableDeclarators()
        .filter(VariableDeclaratorCollection.filters.requiresModule('module'));

      expect(declarators.length).toBe(1);
    })",87,93,snuts
/src/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':102,'endLine':102}","it('accepts multiple module names', function() {
      const declarators = Collection.fromNodes(nodes)
        .findVariableDeclarators()
        .filter(VariableDeclaratorCollection.filters.requiresModule(
          ['module', 'module2']
        ));

      expect(declarators.length).toBe(2);
    })",95,103,snuts
/src/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':117,'endLine':117}","it('renames variable declarations considering scope', function() {
      Collection.fromNodes(nodes)
        .findVariableDeclarators()
        .filter(VariableDeclaratorCollection.filters.requiresModule('module'))
        .renameTo('xyz');

      const identifiers =
        Collection.fromNodes(nodes)
        .find(types.Identifier, {name: 'xyz'});

      expect(identifiers.length).toBe(6);
    })",107,118,snuts
/src/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':129,'endLine':129}","it('does not rename things that are not variables', function() {
      Collection.fromNodes(nodes)
        .findVariableDeclarators('blah')
        .renameTo('blarg');

      const identifiers =
        Collection.fromNodes(nodes)
        .find(types.Identifier, {name: 'blarg'});

      expect(identifiers.length).toBe(1);
    })",120,130,snuts
/src/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':148,'endLine':150}","it('properly renames a shorthand property that was using the old variable name', function() {
      nodes = [recast.parse([
        'var foo = 42;',
        'var obj2 = {',
        '  foo,',
        '};',
      ].join('\n'), {parser: getParser()}).program];

      // Outputs:
      // var newFoo = 42;
      // var obj2 = {
      //   foo: newFoo,
      // };
      Collection.fromNodes(nodes)
        .findVariableDeclarators('foo').renameTo('newFoo');

      expect(
        Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
      ).toBe(2);
      expect(
        Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
      ).toBe(1);

      expect(
        Collection.fromNodes(nodes).find(types.Property).filter(prop => !prop.value.shorthand).length
      ).toBe(1);
      expect(
        Collection.fromNodes(nodes).find(types.Property).filter(prop => prop.value.shorthand).length
      ).toBe(0);
    })",132,161,snuts
/src/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':151,'endLine':153}","it('properly renames a shorthand property that was using the old variable name', function() {
      nodes = [recast.parse([
        'var foo = 42;',
        'var obj2 = {',
        '  foo,',
        '};',
      ].join('\n'), {parser: getParser()}).program];

      // Outputs:
      // var newFoo = 42;
      // var obj2 = {
      //   foo: newFoo,
      // };
      Collection.fromNodes(nodes)
        .findVariableDeclarators('foo').renameTo('newFoo');

      expect(
        Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
      ).toBe(2);
      expect(
        Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
      ).toBe(1);

      expect(
        Collection.fromNodes(nodes).find(types.Property).filter(prop => !prop.value.shorthand).length
      ).toBe(1);
      expect(
        Collection.fromNodes(nodes).find(types.Property).filter(prop => prop.value.shorthand).length
      ).toBe(0);
    })",132,161,snuts
/src/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':155,'endLine':157}","it('properly renames a shorthand property that was using the old variable name', function() {
      nodes = [recast.parse([
        'var foo = 42;',
        'var obj2 = {',
        '  foo,',
        '};',
      ].join('\n'), {parser: getParser()}).program];

      // Outputs:
      // var newFoo = 42;
      // var obj2 = {
      //   foo: newFoo,
      // };
      Collection.fromNodes(nodes)
        .findVariableDeclarators('foo').renameTo('newFoo');

      expect(
        Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
      ).toBe(2);
      expect(
        Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
      ).toBe(1);

      expect(
        Collection.fromNodes(nodes).find(types.Property).filter(prop => !prop.value.shorthand).length
      ).toBe(1);
      expect(
        Collection.fromNodes(nodes).find(types.Property).filter(prop => prop.value.shorthand).length
      ).toBe(0);
    })",132,161,snuts
/src/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':158,'endLine':160}","it('properly renames a shorthand property that was using the old variable name', function() {
      nodes = [recast.parse([
        'var foo = 42;',
        'var obj2 = {',
        '  foo,',
        '};',
      ].join('\n'), {parser: getParser()}).program];

      // Outputs:
      // var newFoo = 42;
      // var obj2 = {
      //   foo: newFoo,
      // };
      Collection.fromNodes(nodes)
        .findVariableDeclarators('foo').renameTo('newFoo');

      expect(
        Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
      ).toBe(2);
      expect(
        Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
      ).toBe(1);

      expect(
        Collection.fromNodes(nodes).find(types.Property).filter(prop => !prop.value.shorthand).length
      ).toBe(1);
      expect(
        Collection.fromNodes(nodes).find(types.Property).filter(prop => prop.value.shorthand).length
      ).toBe(0);
    })",132,161,snuts
/src/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':171,'endLine':171}","it('does not rename React component prop name', function () {
      Collection.fromNodes(nodes)
        .findVariableDeclarators('foo')
        .renameTo('xyz');

      const identifiers = Collection.fromNodes(nodes)
        .find(types.JSXIdentifier, { name: 'foo' });

      expect(identifiers.length).toBe(1);
    })",163,172,snuts
/src/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':187,'endLine':187}","it('does not rename JSX open and closing tags that start with a lowercase letter', function () {
      nodes = [recast.parse([
        'var span = useRef(null);',
        'var element = <span ref={span}></span>;',
      ].join('\n'), {parser: getParser()}).program];

      Collection.fromNodes(nodes)
        .findVariableDeclarators('span')
        .renameTo('spanRef');

      const identifiers = Collection.fromNodes(nodes)
        .find(types.JSXIdentifier, { name: 'spanRef' });

      expect(identifiers.length).toBe(0);
    })",174,188,snuts
/src/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':204,'endLine':204}","it('does rename JSX open and closing tags that are capitalized', function () {
      nodes = [recast.parse([
        'var Span = require(""./Span"");',
        'var span = useRef(null);',
        'var element = <Span ref={span}></Span>;',
      ].join('\n'), {parser: getParser()}).program];

      Collection.fromNodes(nodes)
        .findVariableDeclarators('Span')
        .renameTo('SpanComponent');

      const identifiers = Collection.fromNodes(nodes)
        .find(types.JSXIdentifier, { name: 'SpanComponent' });

      expect(identifiers.length).toBe(2);
    })",190,205,snuts
/src/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':218,'endLine':220}","it('does not rename object property', function () {
        nodes = [
          recast.parse('var foo = 42; var obj = { foo: null };', {
            parser: getParser('babylon'),
          }).program
        ];
        Collection
          .fromNodes(nodes)
          .findVariableDeclarators('foo').renameTo('newFoo');

        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
        ).toBe(1);
        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
        ).toBe(1);
      })",208,224,snuts
/src/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':221,'endLine':223}","it('does not rename object property', function () {
        nodes = [
          recast.parse('var foo = 42; var obj = { foo: null };', {
            parser: getParser('babylon'),
          }).program
        ];
        Collection
          .fromNodes(nodes)
          .findVariableDeclarators('foo').renameTo('newFoo');

        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
        ).toBe(1);
        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
        ).toBe(1);
      })",208,224,snuts
/src/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':236,'endLine':238}","it('does not rename object method', function () {
        nodes = [
          recast.parse('var foo = 42; var obj = { foo() {} };', {
            parser: getParser('babylon'),
          }).program
        ];
        Collection
          .fromNodes(nodes)
          .findVariableDeclarators('foo').renameTo('newFoo');

        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
        ).toBe(1);
        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
        ).toBe(1);
      })",226,242,snuts
/src/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':239,'endLine':241}","it('does not rename object method', function () {
        nodes = [
          recast.parse('var foo = 42; var obj = { foo() {} };', {
            parser: getParser('babylon'),
          }).program
        ];
        Collection
          .fromNodes(nodes)
          .findVariableDeclarators('foo').renameTo('newFoo');

        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
        ).toBe(1);
        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
        ).toBe(1);
      })",226,242,snuts
/src/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':254,'endLine':256}","it('does not rename class method', function () {
        nodes = [
          recast.parse('var foo = 42; class A { foo() {} }', {
            parser: getParser('babylon'),
          }).program
        ];
        Collection
          .fromNodes(nodes)
          .findVariableDeclarators('foo').renameTo('newFoo');

        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
        ).toBe(1);
        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
        ).toBe(1);
      })",244,260,snuts
/src/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':257,'endLine':259}","it('does not rename class method', function () {
        nodes = [
          recast.parse('var foo = 42; class A { foo() {} }', {
            parser: getParser('babylon'),
          }).program
        ];
        Collection
          .fromNodes(nodes)
          .findVariableDeclarators('foo').renameTo('newFoo');

        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
        ).toBe(1);
        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
        ).toBe(1);
      })",244,260,snuts
/src/collections/__tests__/Node-test.js,GeneralFixture,"{'startLine':23,'endLine':23}","beforeEach(function() {
    jest.resetModules();

    Collection = require('../../Collection');
    NodeCollection = require('../Node');
    recast = require('recast');

    types = recast.types.namedTypes;
    b = recast.types.builders;

    NodeCollection.register();

    ast = b.program([
      b.variableDeclaration(
        'var',
        [b.variableDeclarator(b.identifier('foo'), null)]
      ),
      b.functionDeclaration(
        b.identifier('f'),
        [],
        b.blockStatement([
          b.variableDeclaration(
            'var',
            [b.variableDeclarator(b.identifier('bar'), null)]
          ),
          b.variableDeclaration(
            'var',
            [b.variableDeclarator(b.identifier('baz'), null)]
          )
        ])
      ),
    ]);
  })",19,51,snuts
/src/collections/__tests__/Node-test.js,GeneralFixture,"{'startLine':24,'endLine':24}","beforeEach(function() {
    jest.resetModules();

    Collection = require('../../Collection');
    NodeCollection = require('../Node');
    recast = require('recast');

    types = recast.types.namedTypes;
    b = recast.types.builders;

    NodeCollection.register();

    ast = b.program([
      b.variableDeclaration(
        'var',
        [b.variableDeclarator(b.identifier('foo'), null)]
      ),
      b.functionDeclaration(
        b.identifier('f'),
        [],
        b.blockStatement([
          b.variableDeclaration(
            'var',
            [b.variableDeclarator(b.identifier('bar'), null)]
          ),
          b.variableDeclaration(
            'var',
            [b.variableDeclarator(b.identifier('baz'), null)]
          )
        ])
      ),
    ]);
  })",19,51,snuts
/src/collections/__tests__/Node-test.js,IdenticalTestDescription,"{'startLine':345,'endLine':360}","it('accepts an array of nodes', function() {
        const ast = b.variableDeclaration( // eslint-disable-line no-shadow
          'var',
          [b.variableDeclarator(b.identifier('foo'), null)]
        );
        const one = b.variableDeclarator(b.identifier('one'), null);
        const two = b.variableDeclarator(b.identifier('two'), null);

        Collection.fromNodes([ast])
          .find(types.VariableDeclarator)
          .insertAfter([one, two]);

        expect(ast.declarations.length).toBe(3);
        expect(ast.declarations[1]).toBe(one);
        expect(ast.declarations[2]).toBe(two);
      })",345,360,snuts
/src/collections/__tests__/Node-test.js,IdenticalTestDescription,"{'startLine':362,'endLine':378}","it('accepts a function', function() {
        const x = b.identifier('x');
        const foo = b.identifier('foo');
        const bar = b.identifier('bar');
        const ast = b.sequenceExpression([foo, bar]); // eslint-disable-line no-shadow

        Collection.fromNodes([ast])
          .find(types.Identifier)
          .insertAfter(function() {
            return x;
          });

        expect(ast.expressions.length).toBe(4);
        expect(ast.expressions).toEqual([foo, x, bar, x]);
        expect(ast.expressions[1]).toBe(x);
        expect(ast.expressions[3]).toBe(x);
      })",362,378,snuts
/src/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':63,'endLine':63}","it('finds nodes by type', function() {
        const ast = b.sequenceExpression([ // eslint-disable-line no-shadow
          b.identifier('foo'),
          b.literal('asd'),
          b.identifier('bar'),
        ]);
        const vars = Collection.fromNodes([ast]).find(types.Identifier);

        expect(vars.length).toBe(2);
      })",55,64,snuts
/src/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':74,'endLine':74}","it('doesn\'t find the nodes in the collection itself', function() {
        const nodes = [
          b.identifier('foo'),
          b.literal('asd'),
          b.identifier('bar'),
        ];
        const vars = Collection.fromNodes(nodes).find(types.Identifier);

        expect(vars.length).toBe(0);
      })",66,75,snuts
/src/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':86,'endLine':86}","it('finds nodes by type and properties', function() {
        const ast = b.sequenceExpression([ // eslint-disable-line no-shadow
          b.identifier('foo'),
          b.literal('asd'),
          b.identifier('bar'),
        ]);
        const vars = Collection.fromNodes([ast])
          .find(types.Identifier, {name: 'bar'});

        expect(vars.length).toBe(1);
        expect(vars.nodes()[0]).toBe(ast.expressions[2]);
      })",77,88,snuts
/src/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':95,'endLine':95}","it('handles chained find calls properly', function() {
        const vars = Collection.fromNodes([ast])
          .find(types.FunctionDeclaration)
          .find(types.VariableDeclarator, {id: {name: 'bar'}});

        expect(vars.length).toBe(1);
        expect(vars.nodes()[0]).toBe(
          ast.body[1].body.body[0].declarations[0]
        );
      })",90,99,snuts
/src/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':111,'endLine':111}","it('handles multi chain find calls properly', function() {
        const functionBody = ast.body[1].body.body;
        const functionDeclarations = Collection.fromNodes([ast])
          .find(types.FunctionDeclaration);

        const bar = functionDeclarations
          .find(types.VariableDeclarator, {id: {name: 'bar'}});
        const baz = functionDeclarations
          .find(types.VariableDeclarator, {id: {name: 'baz'}});

        expect(bar.length).toBe(1);
        expect(bar.nodes()[0]).toBe(functionBody[0].declarations[0]);
        expect(baz.length).toBe(1);
        expect(baz.nodes()[0]).toBe(functionBody[1].declarations[0]);
      })",101,115,snuts
/src/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':113,'endLine':113}","it('handles multi chain find calls properly', function() {
        const functionBody = ast.body[1].body.body;
        const functionDeclarations = Collection.fromNodes([ast])
          .find(types.FunctionDeclaration);

        const bar = functionDeclarations
          .find(types.VariableDeclarator, {id: {name: 'bar'}});
        const baz = functionDeclarations
          .find(types.VariableDeclarator, {id: {name: 'baz'}});

        expect(bar.length).toBe(1);
        expect(bar.nodes()[0]).toBe(functionBody[0].declarations[0]);
        expect(baz.length).toBe(1);
        expect(baz.nodes()[0]).toBe(functionBody[1].declarations[0]);
      })",101,115,snuts
/src/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':156,'endLine':156}","it('finds closest node (up the tree) of the given type', function() {
        const functionDeclaration = ast.body[1];
        decl = Collection.fromNodes([ast])
          .find(types.Identifier)
          .closest(types.FunctionDeclaration);

        expect(decl.length).toBe(1);
        expect(decl.nodes()[0]).toBe(functionDeclaration);
      })",150,158,snuts
/src/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':190,'endLine':190}","it('fails when filter evaluates as false', function() {
        const literals = Collection.fromNodes([decl])
          .find(types.Literal);
        expect(literals.get(0).node.value).toBe(3);
        const closest = literals.closest(
          types.FunctionDeclaration,
          (node) => node.id && node.id.name === 'blue'
        );
        expect(closest.nodes().length).toBe(0);
      })",182,191,snuts
/src/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':207,'endLine':207}","it('gets the variable declarators for each selected path', function() {
        const variableDeclarator =
          b.variableDeclarator(b.identifier('foo'), null);
        const program = b.program([
          b.variableDeclaration('var', [variableDeclarator]),
          b.expressionStatement(b.identifier('foo')),
          b.expressionStatement(b.identifier('bar'))
        ]);

        const decl = Collection.fromNodes([program])
          .find(types.Identifier)
          .getVariableDeclarators(p => p.value.name);
        expect(decl.length).toBe(1);
        expect(decl.nodes()[0]).toBe(variableDeclarator);
      })",195,209,snuts
/src/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':268,'endLine':268}","it('accepts a function as replacement ', function() {
        const ast = b.sequenceExpression([ // eslint-disable-line no-shadow
          b.identifier('foo'),
          b.literal('asd'),
          b.identifier('bar'),
        ]);

        const expectedArgs = [b.identifier('foo'), b.identifier('bar')];
        const receivedArgs = [];
        const replaceFunction =
          jest.fn(function(path, i) {
            // We have to keep a reference to the argument before it gets
            // replaced
            receivedArgs.push(path.value);
            return b.identifier(path.value.name + i);
          });

        const S = Collection.fromNodes([ast]);
        S.find(types.Identifier)
         .replaceWith(replaceFunction);

        expect(replaceFunction.mock.calls.length).toBe(2);
        expect(receivedArgs).toEqual(expectedArgs);
        // baz is properly replaced
        expect(S.nodes()[0].expressions[0]).toEqual(b.identifier('foo0'));
        // baz1 is properly replaced
        expect(S.nodes()[0].expressions[2]).toEqual(b.identifier('bar1'));
      })",247,274,snuts
/src/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':289,'endLine':289}","it('inserts a new node before the current one', function() {
        const ast = b.variableDeclaration( // eslint-disable-line no-shadow
          'var',
          [b.variableDeclarator(b.identifier('foo'), null)]
        );
        const one = b.variableDeclarator(b.identifier('one'), null);

        Collection.fromNodes([ast])
          .find(types.VariableDeclarator)
          .insertBefore(one);

        expect(ast.declarations.length).toBe(2);
        expect(ast.declarations[0]).toBe(one);
      })",278,291,snuts
/src/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':305,'endLine':305}","it('accepts an array of nodes', function() {
        const ast = b.variableDeclaration( // eslint-disable-line no-shadow
          'var',
          [b.variableDeclarator(b.identifier('foo'), null)]
        );
        const one = b.variableDeclarator(b.identifier('one'), null);
        const two = b.variableDeclarator(b.identifier('two'), null);

        Collection.fromNodes([ast])
          .find(types.VariableDeclarator)
          .insertBefore([one, two]);

        expect(ast.declarations.length).toBe(3);
        expect(ast.declarations[0]).toBe(one);
        expect(ast.declarations[1]).toBe(two);
      })",293,308,snuts
/src/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':322,'endLine':322}","it('accepts a function', function() {
        const x = b.identifier('x');
        const foo = b.identifier('foo');
        const bar = b.identifier('bar');
        const ast = b.sequenceExpression([foo, bar]); // eslint-disable-line no-shadow

        Collection.fromNodes([ast])
          .find(types.Identifier)
          .insertBefore(function() {
            return x;
          });

        expect(ast.expressions.length).toBe(4);
        expect(ast.expressions).toEqual([x, foo, x, bar]);
        expect(ast.expressions[0]).toBe(x);
        expect(ast.expressions[2]).toBe(x);
      })",310,326,snuts
/src/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':341,'endLine':341}","it('inserts a new node after the current one', function() {
        const ast = b.variableDeclaration( // eslint-disable-line no-shadow
          'var',
          [b.variableDeclarator(b.identifier('foo'), null)]
        );
        const one = b.variableDeclarator(b.identifier('one'), null);

        Collection.fromNodes([ast])
          .find(types.VariableDeclarator)
          .insertAfter(one);

        expect(ast.declarations.length).toBe(2);
        expect(ast.declarations[1]).toBe(one);
      })",330,343,snuts
/src/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':357,'endLine':357}","it('accepts an array of nodes', function() {
        const ast = b.variableDeclaration( // eslint-disable-line no-shadow
          'var',
          [b.variableDeclarator(b.identifier('foo'), null)]
        );
        const one = b.variableDeclarator(b.identifier('one'), null);
        const two = b.variableDeclarator(b.identifier('two'), null);

        Collection.fromNodes([ast])
          .find(types.VariableDeclarator)
          .insertAfter([one, two]);

        expect(ast.declarations.length).toBe(3);
        expect(ast.declarations[1]).toBe(one);
        expect(ast.declarations[2]).toBe(two);
      })",345,360,snuts
/src/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':374,'endLine':374}","it('accepts a function', function() {
        const x = b.identifier('x');
        const foo = b.identifier('foo');
        const bar = b.identifier('bar');
        const ast = b.sequenceExpression([foo, bar]); // eslint-disable-line no-shadow

        Collection.fromNodes([ast])
          .find(types.Identifier)
          .insertAfter(function() {
            return x;
          });

        expect(ast.expressions.length).toBe(4);
        expect(ast.expressions).toEqual([foo, x, bar, x]);
        expect(ast.expressions[1]).toBe(x);
        expect(ast.expressions[3]).toBe(x);
      })",362,378,snuts
/src/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':391,'endLine':391}","it('removes a node if it is part of the body of a statement', function() {
        const x = b.expressionStatement(b.identifier('x'));
        const y = b.expressionStatement(b.identifier('y'));
        const ast = b.program([x, y]); // eslint-disable-line no-shadow

        Collection.fromNodes([ast])
          .find(types.Identifier, {name: 'x'})
          .remove();

        expect(ast.body.length).toBe(1);
        expect(ast.body[0]).toBe(y);
      })",382,393,snuts
/src/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':406,'endLine':406}","it('removes a node if it is a function param', function() {
        const x = b.identifier('x');
        const y = b.identifier('y');
        const ast = b.arrowFunctionExpression( // eslint-disable-line no-shadow
          [x, b.identifier('z')],
          y
        );

        Collection.fromNodes([ast])
          .find(types.Identifier, {name: 'z'})
          .remove();
        expect(ast.params.length).toBe(1);
        expect(ast.params[0]).toBe(x);
        expect(ast.body).toBe(y);
      })",395,409,snuts
/src/collections/__tests__/JSXElement-test.js,GeneralFixture,"{'startLine':26,'endLine':26}","beforeEach(function() {
    jest.resetModules();

    Collection = require('../../Collection');
    JSXElementCollection = require('../JSXElement');
    recast = require('recast');
    types = recast.types.namedTypes;
    b = recast.types.builders;

    JSXElementCollection.register();

    nodes = [recast.parse([
      'var FooBar = require(""XYZ"");',
      '<FooBar foo=""bar"" bar=""foo"">',
      '  <Child id=""1"" foo=""bar"" baz>',
      '     <Child />',
      '     <Baz.Bar />',
      '  </Child>',
      '  <Child id=""2"" foo=""baz"" baz/>',
      '  {""foo""}',
      '</FooBar>'
    ].join('\n'), {parser: getParser()}).program];
  })",21,43,snuts
/src/collections/__tests__/JSXElement-test.js,SubOptimalAssert,"{'startLine':55,'endLine':55}","it('lets us find JSXElements by name conveniently', function() {
      const jsx = Collection.fromNodes(nodes).findJSXElements('Child');

      expect(jsx.length).toBe(3);
    })",52,56,snuts
/src/collections/__tests__/JSXElement-test.js,SubOptimalAssert,"{'startLine':61,'endLine':61}","it('finds JSXElements by module name', function() {
      const jsx = Collection.fromNodes(nodes).findJSXElementsByModuleName('XYZ');

      expect(jsx.length).toBe(1);
    })",58,62,snuts
/src/collections/__tests__/JSXElement-test.js,SubOptimalAssert,"{'startLine':69,'endLine':69}","it('returns the child nodes of an JSXElement', function() {
      const children =
        Collection.fromNodes(nodes)
        .findJSXElements('FooBar')
        .childNodes();
      expect(children.length).toBe(7);
      expect(children.getTypes()).toContain('Expression');
    })",64,71,snuts
/src/collections/__tests__/JSXElement-test.js,SubOptimalAssert,"{'startLine':79,'endLine':79}","it('returns the child JSXElements of an JSXElement', function() {
      const children =
        Collection.fromNodes(nodes)
        .findJSXElements('FooBar')
        .childElements();

      expect(children.length).toBe(2);
      expect(children.getTypes()).toContain('JSXElement');
    })",73,81,snuts
/src/collections/__tests__/JSXElement-test.js,SubOptimalAssert,"{'startLine':89,'endLine':89}","it('returns the child element types of an JSXElement', function() {
      const children =
        Collection.fromNodes(nodes)
        .findJSXElements('FooBar')
        .childNodesOfType(types.JSXExpressionContainer);

      expect(children.length).toBe(1);
      expect(children.getTypes()).toContain('JSXExpressionContainer');
    })",83,91,snuts
/src/collections/__tests__/JSXElement-test.js,SubOptimalAssert,"{'startLine':99,'endLine':99}","it('returns a properly typed collection even if empty', function() {
      const children =
        Collection.fromNodes([])
        .findJSXElements('Foo')
        .childElements();

      expect(children.length).toBe(0);
      expect(children.getTypes()).toContain('JSXElement');
    })",93,101,snuts
/src/collections/__tests__/JSXElement-test.js,SubOptimalAssert,"{'startLine':109,'endLine':109}","it('filters elements by attributes', function() {
      const jsx = Collection.fromNodes(nodes)
        .findJSXElements()
        .filter(JSXElementCollection.filters.hasAttributes({foo: 'bar'}));
      expect(jsx.length).toBe(2);
    })",105,110,snuts
/src/collections/__tests__/JSXElement-test.js,SubOptimalAssert,"{'startLine':121,'endLine':121}","it('accepts callback functions as attribute filters', function() {
      const jsx = Collection.fromNodes(nodes)
        .findJSXElements()
        .filter(JSXElementCollection.filters.hasAttributes(
            {
              foo: v => ['bar', 'baz'].indexOf(v) > -1,
              baz: v => v === null
            }
        ));
      expect(jsx.length).toBe(2);
    })",112,122,snuts
/src/collections/__tests__/JSXElement-test.js,SubOptimalAssert,"{'startLine':128,'endLine':128}","it('filters elements by children', function() {
      const jsx = Collection.fromNodes(nodes)
        .findJSXElements()
        .filter(JSXElementCollection.filters.hasChildren('Child'));
      expect(jsx.length).toBe(2);
    })",124,129,snuts
/src/collections/__tests__/JSXElement-test.js,SubOptimalAssert,"{'startLine':138,'endLine':138}","it('gets the root names of JSXElements', function() {
      const names = Collection.fromNodes(nodes)
        .findJSXElements()
        .paths().map(JSXElementCollection.mappings.getRootName);

      expect(names.indexOf('FooBar') > -1).toBe(true);
      expect(names.indexOf('Child') > -1).toBe(true);
      expect(names.indexOf('Baz') > -1).toBe(true);
    })",133,141,snuts
/src/collections/__tests__/JSXElement-test.js,SubOptimalAssert,"{'startLine':139,'endLine':139}","it('gets the root names of JSXElements', function() {
      const names = Collection.fromNodes(nodes)
        .findJSXElements()
        .paths().map(JSXElementCollection.mappings.getRootName);

      expect(names.indexOf('FooBar') > -1).toBe(true);
      expect(names.indexOf('Child') > -1).toBe(true);
      expect(names.indexOf('Baz') > -1).toBe(true);
    })",133,141,snuts
/src/collections/__tests__/JSXElement-test.js,SubOptimalAssert,"{'startLine':140,'endLine':140}","it('gets the root names of JSXElements', function() {
      const names = Collection.fromNodes(nodes)
        .findJSXElements()
        .paths().map(JSXElementCollection.mappings.getRootName);

      expect(names.indexOf('FooBar') > -1).toBe(true);
      expect(names.indexOf('Child') > -1).toBe(true);
      expect(names.indexOf('Baz') > -1).toBe(true);
    })",133,141,snuts
/src/collections/__tests__/JSXElement-test.js,SubOptimalAssert,"{'startLine':162,'endLine':162}","it('handles insertions before children correctly', function() {
      const childElement = b.jsxElement(
        b.jsxOpeningElement(b.jsxIdentifier('Bar'), [], true)
      );
      const newChildElement = b.jsxElement(
        b.jsxOpeningElement(b.jsxIdentifier('Baz'), [], true)
      );
      const literal = b.literal('\n  ');
      const ast = b.jsxElement(
        b.jsxOpeningElement(b.jsxIdentifier('Foo')),
        b.jsxClosingElement(b.jsxIdentifier('Foo')),
        [literal, childElement, literal, childElement, b.literal('\n')]
      );

      Collection.fromNodes([ast])
        .childElements().at(1).insertBefore(newChildElement);

      expect(ast.children.length).toBe(6);
      expect(ast.children[3]).toBe(newChildElement);
    })",145,164,snuts
/src/collections/__tests__/ImportDeclaration-test.js,GeneralFixture,"{'startLine':24,'endLine':24}","beforeEach(function () {
    jest.resetModules();

    Collection = require(""../../Collection"");
    ImportDeclarationCollection = require(""../ImportDeclaration"");
    recast = require(""recast"");
    types = recast.types.namedTypes;
    b = recast.types.builders;

    ImportDeclarationCollection.register();

    nodes = [
      recast.parse(
        [
          'import FooBar from ""XYZ"";',
          'import Foo, { Bar, Baz } from ""@meta/foo"";',
          'import { Bar as Burger } from ""@meta/bar"";',
        ].join(""\n""),
        { parser: getParser() }
      ).program,
    ];
  })",20,41,snuts
/src/collections/__tests__/ImportDeclaration-test.js,GeneralFixture,"{'startLine':25,'endLine':25}","beforeEach(function () {
    jest.resetModules();

    Collection = require(""../../Collection"");
    ImportDeclarationCollection = require(""../ImportDeclaration"");
    recast = require(""recast"");
    types = recast.types.namedTypes;
    b = recast.types.builders;

    ImportDeclarationCollection.register();

    nodes = [
      recast.parse(
        [
          'import FooBar from ""XYZ"";',
          'import Foo, { Bar, Baz } from ""@meta/foo"";',
          'import { Bar as Burger } from ""@meta/bar"";',
        ].join(""\n""),
        { parser: getParser() }
      ).program,
    ];
  })",20,41,snuts
/src/collections/__tests__/ImportDeclaration-test.js,GeneralFixture,"{'startLine':26,'endLine':26}","beforeEach(function () {
    jest.resetModules();

    Collection = require(""../../Collection"");
    ImportDeclarationCollection = require(""../ImportDeclaration"");
    recast = require(""recast"");
    types = recast.types.namedTypes;
    b = recast.types.builders;

    ImportDeclarationCollection.register();

    nodes = [
      recast.parse(
        [
          'import FooBar from ""XYZ"";',
          'import Foo, { Bar, Baz } from ""@meta/foo"";',
          'import { Bar as Burger } from ""@meta/bar"";',
        ].join(""\n""),
        { parser: getParser() }
      ).program,
    ];
  })",20,41,snuts
/src/collections/__tests__/ImportDeclaration-test.js,SubOptimalAssert,"{'startLine':65,'endLine':65}","it(""lets us find ImportDeclarations by source path conveniently"", function () {
        const imports =
          Collection.fromNodes(nodes).findImportDeclarations(""XYZ"");

        expect(imports.length).toBe(1);
      })",61,66,snuts
/src/collections/__tests__/ImportDeclaration-test.js,SubOptimalAssert,"{'startLine':72,'endLine':72}","it(""returns an empty ImportDeclarationCollection if no ImportDeclarations are found"", function () {
        const imports =
          Collection.fromNodes(nodes).findImportDeclarations(""Foo"");

        expect(imports.length).toBe(0);
      })",68,73,snuts
/src/collections/__tests__/ImportDeclaration-test.js,SubOptimalAssert,"{'startLine':84,'endLine':84}","it(""renames an ImportDeclaration with the given sourcePath"", function () {
        Collection.fromNodes(nodes).renameImportDeclaration(""XYZ"", ""ABC"");

        {
          const imports =
            Collection.fromNodes(nodes).findImportDeclarations(""ABC"");

          expect(imports.length).toBe(1);
        }
        {
          const imports =
            Collection.fromNodes(nodes).findImportDeclarations(""XYZ"");
          expect(imports.length).toBe(0);
        }
      })",77,91,snuts
/src/collections/__tests__/ImportDeclaration-test.js,SubOptimalAssert,"{'startLine':89,'endLine':89}","it(""renames an ImportDeclaration with the given sourcePath"", function () {
        Collection.fromNodes(nodes).renameImportDeclaration(""XYZ"", ""ABC"");

        {
          const imports =
            Collection.fromNodes(nodes).findImportDeclarations(""ABC"");

          expect(imports.length).toBe(1);
        }
        {
          const imports =
            Collection.fromNodes(nodes).findImportDeclarations(""XYZ"");
          expect(imports.length).toBe(0);
        }
      })",77,91,snuts
/src/collections/__tests__/ImportDeclaration-test.js,SubOptimalAssert,"{'startLine':117,'endLine':117}","it(""inserts an ImportDeclaration into the AST"", function () {
        Collection.fromNodes(nodes).insertImportDeclaration(""@foo/bar"", [
          b.importDefaultSpecifier(b.identifier(""Foo"")),
          b.importSpecifier(b.identifier(""ABC"")),
          b.importSpecifier(b.identifier(""123"")),
        ]);

        const imports =
          Collection.fromNodes(nodes).findImportDeclarations(""@foo/bar"");

        expect(imports.length).toBe(1);

        const importSpecifiers = imports.paths()[0].value.specifiers;
        expect(importSpecifiers.length).toBe(3);
      })",107,121,snuts
/src/collections/__tests__/ImportDeclaration-test.js,SubOptimalAssert,"{'startLine':120,'endLine':120}","it(""inserts an ImportDeclaration into the AST"", function () {
        Collection.fromNodes(nodes).insertImportDeclaration(""@foo/bar"", [
          b.importDefaultSpecifier(b.identifier(""Foo"")),
          b.importSpecifier(b.identifier(""ABC"")),
          b.importSpecifier(b.identifier(""123"")),
        ]);

        const imports =
          Collection.fromNodes(nodes).findImportDeclarations(""@foo/bar"");

        expect(imports.length).toBe(1);

        const importSpecifiers = imports.paths()[0].value.specifiers;
        expect(importSpecifiers.length).toBe(3);
      })",107,121,snuts
/src/collections/__tests__/ImportDeclaration-test.js,SubOptimalAssert,"{'startLine':137,'endLine':137}","it(""does not insert duplicate ImportDeclarations"", function () {
        Collection.fromNodes(nodes).insertImportDeclaration(""@foo/baz"", [
          b.importDefaultSpecifier(b.identifier(""Foo"")),
          b.importSpecifier(b.identifier(""ABC"")),
        ]);

        Collection.fromNodes(nodes).insertImportDeclaration(""@foo/baz"", [
          b.importDefaultSpecifier(b.identifier(""Foo"")),
          b.importSpecifier(b.identifier(""ABC"")),
        ]);

        const imports =
          Collection.fromNodes(nodes).findImportDeclarations(""@foo/baz"");

        expect(imports.length).toBe(1);
      })",123,138,snuts
/dist/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':68,'endLine':68}","it('finds all variable declarators', function() {
      const declarators = Collection.fromNodes(nodes).findVariableDeclarators();
      expect(declarators.getTypes()).toContain('VariableDeclarator');
      expect(declarators.length).toBe(7);
    })",65,69,snuts
/dist/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':74,'endLine':74}","it('finds variable declarators by name', function() {
      const declarators = Collection.fromNodes(nodes)
        .findVariableDeclarators('bar');
      expect(declarators.length).toBe(2);
    })",71,75,snuts
/dist/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':84,'endLine':84}","it('finds module imports (require)', function() {
      const declarators = Collection.fromNodes(nodes)
        .findVariableDeclarators()
        .filter(VariableDeclaratorCollection.filters.requiresModule());

      expect(declarators.length).toBe(2);
    })",79,85,snuts
/dist/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':92,'endLine':92}","it('finds module imports (require) by module name', function() {
      const declarators = Collection.fromNodes(nodes)
        .findVariableDeclarators()
        .filter(VariableDeclaratorCollection.filters.requiresModule('module'));

      expect(declarators.length).toBe(1);
    })",87,93,snuts
/dist/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':102,'endLine':102}","it('accepts multiple module names', function() {
      const declarators = Collection.fromNodes(nodes)
        .findVariableDeclarators()
        .filter(VariableDeclaratorCollection.filters.requiresModule(
          ['module', 'module2']
        ));

      expect(declarators.length).toBe(2);
    })",95,103,snuts
/dist/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':117,'endLine':117}","it('renames variable declarations considering scope', function() {
      Collection.fromNodes(nodes)
        .findVariableDeclarators()
        .filter(VariableDeclaratorCollection.filters.requiresModule('module'))
        .renameTo('xyz');

      const identifiers =
        Collection.fromNodes(nodes)
        .find(types.Identifier, {name: 'xyz'});

      expect(identifiers.length).toBe(6);
    })",107,118,snuts
/dist/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':129,'endLine':129}","it('does not rename things that are not variables', function() {
      Collection.fromNodes(nodes)
        .findVariableDeclarators('blah')
        .renameTo('blarg');

      const identifiers =
        Collection.fromNodes(nodes)
        .find(types.Identifier, {name: 'blarg'});

      expect(identifiers.length).toBe(1);
    })",120,130,snuts
/dist/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':148,'endLine':150}","it('properly renames a shorthand property that was using the old variable name', function() {
      nodes = [recast.parse([
        'var foo = 42;',
        'var obj2 = {',
        '  foo,',
        '};',
      ].join('\n'), {parser: getParser()}).program];

      // Outputs:
      // var newFoo = 42;
      // var obj2 = {
      //   foo: newFoo,
      // };
      Collection.fromNodes(nodes)
        .findVariableDeclarators('foo').renameTo('newFoo');

      expect(
        Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
      ).toBe(2);
      expect(
        Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
      ).toBe(1);

      expect(
        Collection.fromNodes(nodes).find(types.Property).filter(prop => !prop.value.shorthand).length
      ).toBe(1);
      expect(
        Collection.fromNodes(nodes).find(types.Property).filter(prop => prop.value.shorthand).length
      ).toBe(0);
    })",132,161,snuts
/dist/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':151,'endLine':153}","it('properly renames a shorthand property that was using the old variable name', function() {
      nodes = [recast.parse([
        'var foo = 42;',
        'var obj2 = {',
        '  foo,',
        '};',
      ].join('\n'), {parser: getParser()}).program];

      // Outputs:
      // var newFoo = 42;
      // var obj2 = {
      //   foo: newFoo,
      // };
      Collection.fromNodes(nodes)
        .findVariableDeclarators('foo').renameTo('newFoo');

      expect(
        Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
      ).toBe(2);
      expect(
        Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
      ).toBe(1);

      expect(
        Collection.fromNodes(nodes).find(types.Property).filter(prop => !prop.value.shorthand).length
      ).toBe(1);
      expect(
        Collection.fromNodes(nodes).find(types.Property).filter(prop => prop.value.shorthand).length
      ).toBe(0);
    })",132,161,snuts
/dist/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':155,'endLine':157}","it('properly renames a shorthand property that was using the old variable name', function() {
      nodes = [recast.parse([
        'var foo = 42;',
        'var obj2 = {',
        '  foo,',
        '};',
      ].join('\n'), {parser: getParser()}).program];

      // Outputs:
      // var newFoo = 42;
      // var obj2 = {
      //   foo: newFoo,
      // };
      Collection.fromNodes(nodes)
        .findVariableDeclarators('foo').renameTo('newFoo');

      expect(
        Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
      ).toBe(2);
      expect(
        Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
      ).toBe(1);

      expect(
        Collection.fromNodes(nodes).find(types.Property).filter(prop => !prop.value.shorthand).length
      ).toBe(1);
      expect(
        Collection.fromNodes(nodes).find(types.Property).filter(prop => prop.value.shorthand).length
      ).toBe(0);
    })",132,161,snuts
/dist/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':158,'endLine':160}","it('properly renames a shorthand property that was using the old variable name', function() {
      nodes = [recast.parse([
        'var foo = 42;',
        'var obj2 = {',
        '  foo,',
        '};',
      ].join('\n'), {parser: getParser()}).program];

      // Outputs:
      // var newFoo = 42;
      // var obj2 = {
      //   foo: newFoo,
      // };
      Collection.fromNodes(nodes)
        .findVariableDeclarators('foo').renameTo('newFoo');

      expect(
        Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
      ).toBe(2);
      expect(
        Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
      ).toBe(1);

      expect(
        Collection.fromNodes(nodes).find(types.Property).filter(prop => !prop.value.shorthand).length
      ).toBe(1);
      expect(
        Collection.fromNodes(nodes).find(types.Property).filter(prop => prop.value.shorthand).length
      ).toBe(0);
    })",132,161,snuts
/dist/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':171,'endLine':171}","it('does not rename React component prop name', function () {
      Collection.fromNodes(nodes)
        .findVariableDeclarators('foo')
        .renameTo('xyz');

      const identifiers = Collection.fromNodes(nodes)
        .find(types.JSXIdentifier, { name: 'foo' });

      expect(identifiers.length).toBe(1);
    })",163,172,snuts
/dist/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':187,'endLine':187}","it('does not rename JSX open and closing tags that start with a lowercase letter', function () {
      nodes = [recast.parse([
        'var span = useRef(null);',
        'var element = <span ref={span}></span>;',
      ].join('\n'), {parser: getParser()}).program];

      Collection.fromNodes(nodes)
        .findVariableDeclarators('span')
        .renameTo('spanRef');

      const identifiers = Collection.fromNodes(nodes)
        .find(types.JSXIdentifier, { name: 'spanRef' });

      expect(identifiers.length).toBe(0);
    })",174,188,snuts
/dist/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':204,'endLine':204}","it('does rename JSX open and closing tags that are capitalized', function () {
      nodes = [recast.parse([
        'var Span = require(""./Span"");',
        'var span = useRef(null);',
        'var element = <Span ref={span}></Span>;',
      ].join('\n'), {parser: getParser()}).program];

      Collection.fromNodes(nodes)
        .findVariableDeclarators('Span')
        .renameTo('SpanComponent');

      const identifiers = Collection.fromNodes(nodes)
        .find(types.JSXIdentifier, { name: 'SpanComponent' });

      expect(identifiers.length).toBe(2);
    })",190,205,snuts
/dist/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':218,'endLine':220}","it('does not rename object property', function () {
        nodes = [
          recast.parse('var foo = 42; var obj = { foo: null };', {
            parser: getParser('babylon'),
          }).program
        ];
        Collection
          .fromNodes(nodes)
          .findVariableDeclarators('foo').renameTo('newFoo');

        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
        ).toBe(1);
        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
        ).toBe(1);
      })",208,224,snuts
/dist/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':221,'endLine':223}","it('does not rename object property', function () {
        nodes = [
          recast.parse('var foo = 42; var obj = { foo: null };', {
            parser: getParser('babylon'),
          }).program
        ];
        Collection
          .fromNodes(nodes)
          .findVariableDeclarators('foo').renameTo('newFoo');

        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
        ).toBe(1);
        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
        ).toBe(1);
      })",208,224,snuts
/dist/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':236,'endLine':238}","it('does not rename object method', function () {
        nodes = [
          recast.parse('var foo = 42; var obj = { foo() {} };', {
            parser: getParser('babylon'),
          }).program
        ];
        Collection
          .fromNodes(nodes)
          .findVariableDeclarators('foo').renameTo('newFoo');

        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
        ).toBe(1);
        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
        ).toBe(1);
      })",226,242,snuts
/dist/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':239,'endLine':241}","it('does not rename object method', function () {
        nodes = [
          recast.parse('var foo = 42; var obj = { foo() {} };', {
            parser: getParser('babylon'),
          }).program
        ];
        Collection
          .fromNodes(nodes)
          .findVariableDeclarators('foo').renameTo('newFoo');

        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
        ).toBe(1);
        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
        ).toBe(1);
      })",226,242,snuts
/dist/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':254,'endLine':256}","it('does not rename class method', function () {
        nodes = [
          recast.parse('var foo = 42; class A { foo() {} }', {
            parser: getParser('babylon'),
          }).program
        ];
        Collection
          .fromNodes(nodes)
          .findVariableDeclarators('foo').renameTo('newFoo');

        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
        ).toBe(1);
        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
        ).toBe(1);
      })",244,260,snuts
/dist/collections/__tests__/VariableDeclarator-test.js,SubOptimalAssert,"{'startLine':257,'endLine':259}","it('does not rename class method', function () {
        nodes = [
          recast.parse('var foo = 42; class A { foo() {} }', {
            parser: getParser('babylon'),
          }).program
        ];
        Collection
          .fromNodes(nodes)
          .findVariableDeclarators('foo').renameTo('newFoo');

        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
        ).toBe(1);
        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
        ).toBe(1);
      })",244,260,snuts
/dist/collections/__tests__/Node-test.js,GeneralFixture,"{'startLine':23,'endLine':23}","beforeEach(function() {
    jest.resetModules();

    Collection = require('../../Collection');
    NodeCollection = require('../Node');
    recast = require('recast');

    types = recast.types.namedTypes;
    b = recast.types.builders;

    NodeCollection.register();

    ast = b.program([
      b.variableDeclaration(
        'var',
        [b.variableDeclarator(b.identifier('foo'), null)]
      ),
      b.functionDeclaration(
        b.identifier('f'),
        [],
        b.blockStatement([
          b.variableDeclaration(
            'var',
            [b.variableDeclarator(b.identifier('bar'), null)]
          ),
          b.variableDeclaration(
            'var',
            [b.variableDeclarator(b.identifier('baz'), null)]
          )
        ])
      ),
    ]);
  })",19,51,snuts
/dist/collections/__tests__/Node-test.js,GeneralFixture,"{'startLine':24,'endLine':24}","beforeEach(function() {
    jest.resetModules();

    Collection = require('../../Collection');
    NodeCollection = require('../Node');
    recast = require('recast');

    types = recast.types.namedTypes;
    b = recast.types.builders;

    NodeCollection.register();

    ast = b.program([
      b.variableDeclaration(
        'var',
        [b.variableDeclarator(b.identifier('foo'), null)]
      ),
      b.functionDeclaration(
        b.identifier('f'),
        [],
        b.blockStatement([
          b.variableDeclaration(
            'var',
            [b.variableDeclarator(b.identifier('bar'), null)]
          ),
          b.variableDeclaration(
            'var',
            [b.variableDeclarator(b.identifier('baz'), null)]
          )
        ])
      ),
    ]);
  })",19,51,snuts
/dist/collections/__tests__/Node-test.js,IdenticalTestDescription,"{'startLine':345,'endLine':360}","it('accepts an array of nodes', function() {
        const ast = b.variableDeclaration( // eslint-disable-line no-shadow
          'var',
          [b.variableDeclarator(b.identifier('foo'), null)]
        );
        const one = b.variableDeclarator(b.identifier('one'), null);
        const two = b.variableDeclarator(b.identifier('two'), null);

        Collection.fromNodes([ast])
          .find(types.VariableDeclarator)
          .insertAfter([one, two]);

        expect(ast.declarations.length).toBe(3);
        expect(ast.declarations[1]).toBe(one);
        expect(ast.declarations[2]).toBe(two);
      })",345,360,snuts
/dist/collections/__tests__/Node-test.js,IdenticalTestDescription,"{'startLine':362,'endLine':378}","it('accepts a function', function() {
        const x = b.identifier('x');
        const foo = b.identifier('foo');
        const bar = b.identifier('bar');
        const ast = b.sequenceExpression([foo, bar]); // eslint-disable-line no-shadow

        Collection.fromNodes([ast])
          .find(types.Identifier)
          .insertAfter(function() {
            return x;
          });

        expect(ast.expressions.length).toBe(4);
        expect(ast.expressions).toEqual([foo, x, bar, x]);
        expect(ast.expressions[1]).toBe(x);
        expect(ast.expressions[3]).toBe(x);
      })",362,378,snuts
/dist/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':63,'endLine':63}","it('finds nodes by type', function() {
        const ast = b.sequenceExpression([ // eslint-disable-line no-shadow
          b.identifier('foo'),
          b.literal('asd'),
          b.identifier('bar'),
        ]);
        const vars = Collection.fromNodes([ast]).find(types.Identifier);

        expect(vars.length).toBe(2);
      })",55,64,snuts
/dist/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':74,'endLine':74}","it('doesn\'t find the nodes in the collection itself', function() {
        const nodes = [
          b.identifier('foo'),
          b.literal('asd'),
          b.identifier('bar'),
        ];
        const vars = Collection.fromNodes(nodes).find(types.Identifier);

        expect(vars.length).toBe(0);
      })",66,75,snuts
/dist/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':86,'endLine':86}","it('finds nodes by type and properties', function() {
        const ast = b.sequenceExpression([ // eslint-disable-line no-shadow
          b.identifier('foo'),
          b.literal('asd'),
          b.identifier('bar'),
        ]);
        const vars = Collection.fromNodes([ast])
          .find(types.Identifier, {name: 'bar'});

        expect(vars.length).toBe(1);
        expect(vars.nodes()[0]).toBe(ast.expressions[2]);
      })",77,88,snuts
/dist/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':95,'endLine':95}","it('handles chained find calls properly', function() {
        const vars = Collection.fromNodes([ast])
          .find(types.FunctionDeclaration)
          .find(types.VariableDeclarator, {id: {name: 'bar'}});

        expect(vars.length).toBe(1);
        expect(vars.nodes()[0]).toBe(
          ast.body[1].body.body[0].declarations[0]
        );
      })",90,99,snuts
/dist/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':111,'endLine':111}","it('handles multi chain find calls properly', function() {
        const functionBody = ast.body[1].body.body;
        const functionDeclarations = Collection.fromNodes([ast])
          .find(types.FunctionDeclaration);

        const bar = functionDeclarations
          .find(types.VariableDeclarator, {id: {name: 'bar'}});
        const baz = functionDeclarations
          .find(types.VariableDeclarator, {id: {name: 'baz'}});

        expect(bar.length).toBe(1);
        expect(bar.nodes()[0]).toBe(functionBody[0].declarations[0]);
        expect(baz.length).toBe(1);
        expect(baz.nodes()[0]).toBe(functionBody[1].declarations[0]);
      })",101,115,snuts
/dist/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':113,'endLine':113}","it('handles multi chain find calls properly', function() {
        const functionBody = ast.body[1].body.body;
        const functionDeclarations = Collection.fromNodes([ast])
          .find(types.FunctionDeclaration);

        const bar = functionDeclarations
          .find(types.VariableDeclarator, {id: {name: 'bar'}});
        const baz = functionDeclarations
          .find(types.VariableDeclarator, {id: {name: 'baz'}});

        expect(bar.length).toBe(1);
        expect(bar.nodes()[0]).toBe(functionBody[0].declarations[0]);
        expect(baz.length).toBe(1);
        expect(baz.nodes()[0]).toBe(functionBody[1].declarations[0]);
      })",101,115,snuts
/dist/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':156,'endLine':156}","it('finds closest node (up the tree) of the given type', function() {
        const functionDeclaration = ast.body[1];
        decl = Collection.fromNodes([ast])
          .find(types.Identifier)
          .closest(types.FunctionDeclaration);

        expect(decl.length).toBe(1);
        expect(decl.nodes()[0]).toBe(functionDeclaration);
      })",150,158,snuts
/dist/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':190,'endLine':190}","it('fails when filter evaluates as false', function() {
        const literals = Collection.fromNodes([decl])
          .find(types.Literal);
        expect(literals.get(0).node.value).toBe(3);
        const closest = literals.closest(
          types.FunctionDeclaration,
          (node) => node.id && node.id.name === 'blue'
        );
        expect(closest.nodes().length).toBe(0);
      })",182,191,snuts
/dist/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':207,'endLine':207}","it('gets the variable declarators for each selected path', function() {
        const variableDeclarator =
          b.variableDeclarator(b.identifier('foo'), null);
        const program = b.program([
          b.variableDeclaration('var', [variableDeclarator]),
          b.expressionStatement(b.identifier('foo')),
          b.expressionStatement(b.identifier('bar'))
        ]);

        const decl = Collection.fromNodes([program])
          .find(types.Identifier)
          .getVariableDeclarators(p => p.value.name);
        expect(decl.length).toBe(1);
        expect(decl.nodes()[0]).toBe(variableDeclarator);
      })",195,209,snuts
/dist/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':268,'endLine':268}","it('accepts a function as replacement ', function() {
        const ast = b.sequenceExpression([ // eslint-disable-line no-shadow
          b.identifier('foo'),
          b.literal('asd'),
          b.identifier('bar'),
        ]);

        const expectedArgs = [b.identifier('foo'), b.identifier('bar')];
        const receivedArgs = [];
        const replaceFunction =
          jest.fn(function(path, i) {
            // We have to keep a reference to the argument before it gets
            // replaced
            receivedArgs.push(path.value);
            return b.identifier(path.value.name + i);
          });

        const S = Collection.fromNodes([ast]);
        S.find(types.Identifier)
         .replaceWith(replaceFunction);

        expect(replaceFunction.mock.calls.length).toBe(2);
        expect(receivedArgs).toEqual(expectedArgs);
        // baz is properly replaced
        expect(S.nodes()[0].expressions[0]).toEqual(b.identifier('foo0'));
        // baz1 is properly replaced
        expect(S.nodes()[0].expressions[2]).toEqual(b.identifier('bar1'));
      })",247,274,snuts
/dist/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':289,'endLine':289}","it('inserts a new node before the current one', function() {
        const ast = b.variableDeclaration( // eslint-disable-line no-shadow
          'var',
          [b.variableDeclarator(b.identifier('foo'), null)]
        );
        const one = b.variableDeclarator(b.identifier('one'), null);

        Collection.fromNodes([ast])
          .find(types.VariableDeclarator)
          .insertBefore(one);

        expect(ast.declarations.length).toBe(2);
        expect(ast.declarations[0]).toBe(one);
      })",278,291,snuts
/dist/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':305,'endLine':305}","it('accepts an array of nodes', function() {
        const ast = b.variableDeclaration( // eslint-disable-line no-shadow
          'var',
          [b.variableDeclarator(b.identifier('foo'), null)]
        );
        const one = b.variableDeclarator(b.identifier('one'), null);
        const two = b.variableDeclarator(b.identifier('two'), null);

        Collection.fromNodes([ast])
          .find(types.VariableDeclarator)
          .insertBefore([one, two]);

        expect(ast.declarations.length).toBe(3);
        expect(ast.declarations[0]).toBe(one);
        expect(ast.declarations[1]).toBe(two);
      })",293,308,snuts
/dist/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':322,'endLine':322}","it('accepts a function', function() {
        const x = b.identifier('x');
        const foo = b.identifier('foo');
        const bar = b.identifier('bar');
        const ast = b.sequenceExpression([foo, bar]); // eslint-disable-line no-shadow

        Collection.fromNodes([ast])
          .find(types.Identifier)
          .insertBefore(function() {
            return x;
          });

        expect(ast.expressions.length).toBe(4);
        expect(ast.expressions).toEqual([x, foo, x, bar]);
        expect(ast.expressions[0]).toBe(x);
        expect(ast.expressions[2]).toBe(x);
      })",310,326,snuts
/dist/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':341,'endLine':341}","it('inserts a new node after the current one', function() {
        const ast = b.variableDeclaration( // eslint-disable-line no-shadow
          'var',
          [b.variableDeclarator(b.identifier('foo'), null)]
        );
        const one = b.variableDeclarator(b.identifier('one'), null);

        Collection.fromNodes([ast])
          .find(types.VariableDeclarator)
          .insertAfter(one);

        expect(ast.declarations.length).toBe(2);
        expect(ast.declarations[1]).toBe(one);
      })",330,343,snuts
/dist/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':357,'endLine':357}","it('accepts an array of nodes', function() {
        const ast = b.variableDeclaration( // eslint-disable-line no-shadow
          'var',
          [b.variableDeclarator(b.identifier('foo'), null)]
        );
        const one = b.variableDeclarator(b.identifier('one'), null);
        const two = b.variableDeclarator(b.identifier('two'), null);

        Collection.fromNodes([ast])
          .find(types.VariableDeclarator)
          .insertAfter([one, two]);

        expect(ast.declarations.length).toBe(3);
        expect(ast.declarations[1]).toBe(one);
        expect(ast.declarations[2]).toBe(two);
      })",345,360,snuts
/dist/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':374,'endLine':374}","it('accepts a function', function() {
        const x = b.identifier('x');
        const foo = b.identifier('foo');
        const bar = b.identifier('bar');
        const ast = b.sequenceExpression([foo, bar]); // eslint-disable-line no-shadow

        Collection.fromNodes([ast])
          .find(types.Identifier)
          .insertAfter(function() {
            return x;
          });

        expect(ast.expressions.length).toBe(4);
        expect(ast.expressions).toEqual([foo, x, bar, x]);
        expect(ast.expressions[1]).toBe(x);
        expect(ast.expressions[3]).toBe(x);
      })",362,378,snuts
/dist/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':391,'endLine':391}","it('removes a node if it is part of the body of a statement', function() {
        const x = b.expressionStatement(b.identifier('x'));
        const y = b.expressionStatement(b.identifier('y'));
        const ast = b.program([x, y]); // eslint-disable-line no-shadow

        Collection.fromNodes([ast])
          .find(types.Identifier, {name: 'x'})
          .remove();

        expect(ast.body.length).toBe(1);
        expect(ast.body[0]).toBe(y);
      })",382,393,snuts
/dist/collections/__tests__/Node-test.js,SubOptimalAssert,"{'startLine':406,'endLine':406}","it('removes a node if it is a function param', function() {
        const x = b.identifier('x');
        const y = b.identifier('y');
        const ast = b.arrowFunctionExpression( // eslint-disable-line no-shadow
          [x, b.identifier('z')],
          y
        );

        Collection.fromNodes([ast])
          .find(types.Identifier, {name: 'z'})
          .remove();
        expect(ast.params.length).toBe(1);
        expect(ast.params[0]).toBe(x);
        expect(ast.body).toBe(y);
      })",395,409,snuts
/dist/collections/__tests__/JSXElement-test.js,GeneralFixture,"{'startLine':26,'endLine':26}","beforeEach(function() {
    jest.resetModules();

    Collection = require('../../Collection');
    JSXElementCollection = require('../JSXElement');
    recast = require('recast');
    types = recast.types.namedTypes;
    b = recast.types.builders;

    JSXElementCollection.register();

    nodes = [recast.parse([
      'var FooBar = require(""XYZ"");',
      '<FooBar foo=""bar"" bar=""foo"">',
      '  <Child id=""1"" foo=""bar"" baz>',
      '     <Child />',
      '     <Baz.Bar />',
      '  </Child>',
      '  <Child id=""2"" foo=""baz"" baz/>',
      '  {""foo""}',
      '</FooBar>'
    ].join('\n'), {parser: getParser()}).program];
  })",21,43,snuts
/dist/collections/__tests__/JSXElement-test.js,SubOptimalAssert,"{'startLine':55,'endLine':55}","it('lets us find JSXElements by name conveniently', function() {
      const jsx = Collection.fromNodes(nodes).findJSXElements('Child');

      expect(jsx.length).toBe(3);
    })",52,56,snuts
/dist/collections/__tests__/JSXElement-test.js,SubOptimalAssert,"{'startLine':61,'endLine':61}","it('finds JSXElements by module name', function() {
      const jsx = Collection.fromNodes(nodes).findJSXElementsByModuleName('XYZ');

      expect(jsx.length).toBe(1);
    })",58,62,snuts
/dist/collections/__tests__/JSXElement-test.js,SubOptimalAssert,"{'startLine':69,'endLine':69}","it('returns the child nodes of an JSXElement', function() {
      const children =
        Collection.fromNodes(nodes)
        .findJSXElements('FooBar')
        .childNodes();
      expect(children.length).toBe(7);
      expect(children.getTypes()).toContain('Expression');
    })",64,71,snuts
/dist/collections/__tests__/JSXElement-test.js,SubOptimalAssert,"{'startLine':79,'endLine':79}","it('returns the child JSXElements of an JSXElement', function() {
      const children =
        Collection.fromNodes(nodes)
        .findJSXElements('FooBar')
        .childElements();

      expect(children.length).toBe(2);
      expect(children.getTypes()).toContain('JSXElement');
    })",73,81,snuts
/dist/collections/__tests__/JSXElement-test.js,SubOptimalAssert,"{'startLine':89,'endLine':89}","it('returns the child element types of an JSXElement', function() {
      const children =
        Collection.fromNodes(nodes)
        .findJSXElements('FooBar')
        .childNodesOfType(types.JSXExpressionContainer);

      expect(children.length).toBe(1);
      expect(children.getTypes()).toContain('JSXExpressionContainer');
    })",83,91,snuts
/dist/collections/__tests__/JSXElement-test.js,SubOptimalAssert,"{'startLine':99,'endLine':99}","it('returns a properly typed collection even if empty', function() {
      const children =
        Collection.fromNodes([])
        .findJSXElements('Foo')
        .childElements();

      expect(children.length).toBe(0);
      expect(children.getTypes()).toContain('JSXElement');
    })",93,101,snuts
/dist/collections/__tests__/JSXElement-test.js,SubOptimalAssert,"{'startLine':109,'endLine':109}","it('filters elements by attributes', function() {
      const jsx = Collection.fromNodes(nodes)
        .findJSXElements()
        .filter(JSXElementCollection.filters.hasAttributes({foo: 'bar'}));
      expect(jsx.length).toBe(2);
    })",105,110,snuts
/dist/collections/__tests__/JSXElement-test.js,SubOptimalAssert,"{'startLine':121,'endLine':121}","it('accepts callback functions as attribute filters', function() {
      const jsx = Collection.fromNodes(nodes)
        .findJSXElements()
        .filter(JSXElementCollection.filters.hasAttributes(
            {
              foo: v => ['bar', 'baz'].indexOf(v) > -1,
              baz: v => v === null
            }
        ));
      expect(jsx.length).toBe(2);
    })",112,122,snuts
/dist/collections/__tests__/JSXElement-test.js,SubOptimalAssert,"{'startLine':128,'endLine':128}","it('filters elements by children', function() {
      const jsx = Collection.fromNodes(nodes)
        .findJSXElements()
        .filter(JSXElementCollection.filters.hasChildren('Child'));
      expect(jsx.length).toBe(2);
    })",124,129,snuts
/dist/collections/__tests__/JSXElement-test.js,SubOptimalAssert,"{'startLine':138,'endLine':138}","it('gets the root names of JSXElements', function() {
      const names = Collection.fromNodes(nodes)
        .findJSXElements()
        .paths().map(JSXElementCollection.mappings.getRootName);

      expect(names.indexOf('FooBar') > -1).toBe(true);
      expect(names.indexOf('Child') > -1).toBe(true);
      expect(names.indexOf('Baz') > -1).toBe(true);
    })",133,141,snuts
/dist/collections/__tests__/JSXElement-test.js,SubOptimalAssert,"{'startLine':139,'endLine':139}","it('gets the root names of JSXElements', function() {
      const names = Collection.fromNodes(nodes)
        .findJSXElements()
        .paths().map(JSXElementCollection.mappings.getRootName);

      expect(names.indexOf('FooBar') > -1).toBe(true);
      expect(names.indexOf('Child') > -1).toBe(true);
      expect(names.indexOf('Baz') > -1).toBe(true);
    })",133,141,snuts
/dist/collections/__tests__/JSXElement-test.js,SubOptimalAssert,"{'startLine':140,'endLine':140}","it('gets the root names of JSXElements', function() {
      const names = Collection.fromNodes(nodes)
        .findJSXElements()
        .paths().map(JSXElementCollection.mappings.getRootName);

      expect(names.indexOf('FooBar') > -1).toBe(true);
      expect(names.indexOf('Child') > -1).toBe(true);
      expect(names.indexOf('Baz') > -1).toBe(true);
    })",133,141,snuts
/dist/collections/__tests__/JSXElement-test.js,SubOptimalAssert,"{'startLine':162,'endLine':162}","it('handles insertions before children correctly', function() {
      const childElement = b.jsxElement(
        b.jsxOpeningElement(b.jsxIdentifier('Bar'), [], true)
      );
      const newChildElement = b.jsxElement(
        b.jsxOpeningElement(b.jsxIdentifier('Baz'), [], true)
      );
      const literal = b.literal('\n  ');
      const ast = b.jsxElement(
        b.jsxOpeningElement(b.jsxIdentifier('Foo')),
        b.jsxClosingElement(b.jsxIdentifier('Foo')),
        [literal, childElement, literal, childElement, b.literal('\n')]
      );

      Collection.fromNodes([ast])
        .childElements().at(1).insertBefore(newChildElement);

      expect(ast.children.length).toBe(6);
      expect(ast.children[3]).toBe(newChildElement);
    })",145,164,snuts
/dist/collections/__tests__/ImportDeclaration-test.js,GeneralFixture,"{'startLine':24,'endLine':24}","beforeEach(function () {
    jest.resetModules();

    Collection = require(""../../Collection"");
    ImportDeclarationCollection = require(""../ImportDeclaration"");
    recast = require(""recast"");
    types = recast.types.namedTypes;
    b = recast.types.builders;

    ImportDeclarationCollection.register();

    nodes = [
      recast.parse(
        [
          'import FooBar from ""XYZ"";',
          'import Foo, { Bar, Baz } from ""@meta/foo"";',
          'import { Bar as Burger } from ""@meta/bar"";',
        ].join(""\n""),
        { parser: getParser() }
      ).program,
    ];
  })",20,41,snuts
/dist/collections/__tests__/ImportDeclaration-test.js,GeneralFixture,"{'startLine':25,'endLine':25}","beforeEach(function () {
    jest.resetModules();

    Collection = require(""../../Collection"");
    ImportDeclarationCollection = require(""../ImportDeclaration"");
    recast = require(""recast"");
    types = recast.types.namedTypes;
    b = recast.types.builders;

    ImportDeclarationCollection.register();

    nodes = [
      recast.parse(
        [
          'import FooBar from ""XYZ"";',
          'import Foo, { Bar, Baz } from ""@meta/foo"";',
          'import { Bar as Burger } from ""@meta/bar"";',
        ].join(""\n""),
        { parser: getParser() }
      ).program,
    ];
  })",20,41,snuts
/dist/collections/__tests__/ImportDeclaration-test.js,GeneralFixture,"{'startLine':26,'endLine':26}","beforeEach(function () {
    jest.resetModules();

    Collection = require(""../../Collection"");
    ImportDeclarationCollection = require(""../ImportDeclaration"");
    recast = require(""recast"");
    types = recast.types.namedTypes;
    b = recast.types.builders;

    ImportDeclarationCollection.register();

    nodes = [
      recast.parse(
        [
          'import FooBar from ""XYZ"";',
          'import Foo, { Bar, Baz } from ""@meta/foo"";',
          'import { Bar as Burger } from ""@meta/bar"";',
        ].join(""\n""),
        { parser: getParser() }
      ).program,
    ];
  })",20,41,snuts
/dist/collections/__tests__/ImportDeclaration-test.js,SubOptimalAssert,"{'startLine':65,'endLine':65}","it(""lets us find ImportDeclarations by source path conveniently"", function () {
        const imports =
          Collection.fromNodes(nodes).findImportDeclarations(""XYZ"");

        expect(imports.length).toBe(1);
      })",61,66,snuts
/dist/collections/__tests__/ImportDeclaration-test.js,SubOptimalAssert,"{'startLine':72,'endLine':72}","it(""returns an empty ImportDeclarationCollection if no ImportDeclarations are found"", function () {
        const imports =
          Collection.fromNodes(nodes).findImportDeclarations(""Foo"");

        expect(imports.length).toBe(0);
      })",68,73,snuts
/dist/collections/__tests__/ImportDeclaration-test.js,SubOptimalAssert,"{'startLine':84,'endLine':84}","it(""renames an ImportDeclaration with the given sourcePath"", function () {
        Collection.fromNodes(nodes).renameImportDeclaration(""XYZ"", ""ABC"");

        {
          const imports =
            Collection.fromNodes(nodes).findImportDeclarations(""ABC"");

          expect(imports.length).toBe(1);
        }
        {
          const imports =
            Collection.fromNodes(nodes).findImportDeclarations(""XYZ"");
          expect(imports.length).toBe(0);
        }
      })",77,91,snuts
/dist/collections/__tests__/ImportDeclaration-test.js,SubOptimalAssert,"{'startLine':89,'endLine':89}","it(""renames an ImportDeclaration with the given sourcePath"", function () {
        Collection.fromNodes(nodes).renameImportDeclaration(""XYZ"", ""ABC"");

        {
          const imports =
            Collection.fromNodes(nodes).findImportDeclarations(""ABC"");

          expect(imports.length).toBe(1);
        }
        {
          const imports =
            Collection.fromNodes(nodes).findImportDeclarations(""XYZ"");
          expect(imports.length).toBe(0);
        }
      })",77,91,snuts
/dist/collections/__tests__/ImportDeclaration-test.js,SubOptimalAssert,"{'startLine':117,'endLine':117}","it(""inserts an ImportDeclaration into the AST"", function () {
        Collection.fromNodes(nodes).insertImportDeclaration(""@foo/bar"", [
          b.importDefaultSpecifier(b.identifier(""Foo"")),
          b.importSpecifier(b.identifier(""ABC"")),
          b.importSpecifier(b.identifier(""123"")),
        ]);

        const imports =
          Collection.fromNodes(nodes).findImportDeclarations(""@foo/bar"");

        expect(imports.length).toBe(1);

        const importSpecifiers = imports.paths()[0].value.specifiers;
        expect(importSpecifiers.length).toBe(3);
      })",107,121,snuts
/dist/collections/__tests__/ImportDeclaration-test.js,SubOptimalAssert,"{'startLine':120,'endLine':120}","it(""inserts an ImportDeclaration into the AST"", function () {
        Collection.fromNodes(nodes).insertImportDeclaration(""@foo/bar"", [
          b.importDefaultSpecifier(b.identifier(""Foo"")),
          b.importSpecifier(b.identifier(""ABC"")),
          b.importSpecifier(b.identifier(""123"")),
        ]);

        const imports =
          Collection.fromNodes(nodes).findImportDeclarations(""@foo/bar"");

        expect(imports.length).toBe(1);

        const importSpecifiers = imports.paths()[0].value.specifiers;
        expect(importSpecifiers.length).toBe(3);
      })",107,121,snuts
/dist/collections/__tests__/ImportDeclaration-test.js,SubOptimalAssert,"{'startLine':137,'endLine':137}","it(""does not insert duplicate ImportDeclarations"", function () {
        Collection.fromNodes(nodes).insertImportDeclaration(""@foo/baz"", [
          b.importDefaultSpecifier(b.identifier(""Foo"")),
          b.importSpecifier(b.identifier(""ABC"")),
        ]);

        Collection.fromNodes(nodes).insertImportDeclaration(""@foo/baz"", [
          b.importDefaultSpecifier(b.identifier(""Foo"")),
          b.importSpecifier(b.identifier(""ABC"")),
        ]);

        const imports =
          Collection.fromNodes(nodes).findImportDeclarations(""@foo/baz"");

        expect(imports.length).toBe(1);
      })",123,138,snuts
/src/utils/__tests__/union-test.js,Conditional Test Logic,"{'line': 13, 'column': 2, 'index': 270}",Unknown,,,steel
/src/utils/__tests__/intersection-test.js,Conditional Test Logic,"{'line': 13, 'column': 2, 'index': 284}",Unknown,,,steel
/src/collections/__tests__/VariableDeclarator-test.js,Lazy Test,"{'line': 138, 'column': 29, 'index': 4000}","it('properly renames a shorthand property that was using the old variable name', function() {
      nodes = [recast.parse([
        'var foo = 42;',
        'var obj2 = {',
        '  foo,',
        '};',
      ].join('\n'), {parser: getParser()}).program];

      // Outputs:
      // var newFoo = 42;
      // var obj2 = {
      //   foo: newFoo,
      // };
      Collection.fromNodes(nodes)
        .findVariableDeclarators('foo').renameTo('newFoo');

      expect(
        Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
      ).toBe(2);
      expect(
        Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
      ).toBe(1);

      expect(
        Collection.fromNodes(nodes).find(types.Property).filter(prop => !prop.value.shorthand).length
      ).toBe(1);
      expect(
        Collection.fromNodes(nodes).find(types.Property).filter(prop => prop.value.shorthand).length
      ).toBe(0);
    })",132,161,steel
/src/collections/__tests__/VariableDeclarator-test.js,Lazy Test,"{'line': 178, 'column': 29, 'index': 5301}","it('does not rename JSX open and closing tags that start with a lowercase letter', function () {
      nodes = [recast.parse([
        'var span = useRef(null);',
        'var element = <span ref={span}></span>;',
      ].join('\n'), {parser: getParser()}).program];

      Collection.fromNodes(nodes)
        .findVariableDeclarators('span')
        .renameTo('spanRef');

      const identifiers = Collection.fromNodes(nodes)
        .find(types.JSXIdentifier, { name: 'spanRef' });

      expect(identifiers.length).toBe(0);
    })",174,188,steel
/src/collections/__tests__/VariableDeclarator-test.js,Lazy Test,"{'line': 195, 'column': 29, 'index': 5865}","it('does rename JSX open and closing tags that are capitalized', function () {
      nodes = [recast.parse([
        'var Span = require(""./Span"");',
        'var span = useRef(null);',
        'var element = <Span ref={span}></Span>;',
      ].join('\n'), {parser: getParser()}).program];

      Collection.fromNodes(nodes)
        .findVariableDeclarators('Span')
        .renameTo('SpanComponent');

      const identifiers = Collection.fromNodes(nodes)
        .find(types.JSXIdentifier, { name: 'SpanComponent' });

      expect(identifiers.length).toBe(2);
    })",190,205,steel
/src/collections/__tests__/VariableDeclarator-test.js,Lazy Test,"{'line': 211, 'column': 20, 'index': 6385}","it('does not rename object property', function () {
        nodes = [
          recast.parse('var foo = 42; var obj = { foo: null };', {
            parser: getParser('babylon'),
          }).program
        ];
        Collection
          .fromNodes(nodes)
          .findVariableDeclarators('foo').renameTo('newFoo');

        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
        ).toBe(1);
        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
        ).toBe(1);
      })",208,224,steel
/src/collections/__tests__/VariableDeclarator-test.js,Lazy Test,"{'line': 229, 'column': 20, 'index': 6962}","it('does not rename object method', function () {
        nodes = [
          recast.parse('var foo = 42; var obj = { foo() {} };', {
            parser: getParser('babylon'),
          }).program
        ];
        Collection
          .fromNodes(nodes)
          .findVariableDeclarators('foo').renameTo('newFoo');

        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
        ).toBe(1);
        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
        ).toBe(1);
      })",226,242,steel
/src/collections/__tests__/VariableDeclarator-test.js,Lazy Test,"{'line': 247, 'column': 20, 'index': 7535}","it('does not rename class method', function () {
        nodes = [
          recast.parse('var foo = 42; class A { foo() {} }', {
            parser: getParser('babylon'),
          }).program
        ];
        Collection
          .fromNodes(nodes)
          .findVariableDeclarators('foo').renameTo('newFoo');

        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
        ).toBe(1);
        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
        ).toBe(1);
      })",244,260,steel
/src/collections/__tests__/VariableDeclarator-test.js,Magic Number,"{'line': 68, 'column': 38, 'index': 1852}","it('finds all variable declarators', function() {
      const declarators = Collection.fromNodes(nodes).findVariableDeclarators();
      expect(declarators.getTypes()).toContain('VariableDeclarator');
      expect(declarators.length).toBe(7);
    })",65,69,steel
/src/collections/__tests__/VariableDeclarator-test.js,Magic Number,"{'line': 74, 'column': 38, 'index': 2056}","it('finds variable declarators by name', function() {
      const declarators = Collection.fromNodes(nodes)
        .findVariableDeclarators('bar');
      expect(declarators.length).toBe(2);
    })",71,75,steel
/src/collections/__tests__/VariableDeclarator-test.js,Magic Number,"{'line': 84, 'column': 38, 'index': 2364}","it('finds module imports (require)', function() {
      const declarators = Collection.fromNodes(nodes)
        .findVariableDeclarators()
        .filter(VariableDeclaratorCollection.filters.requiresModule());

      expect(declarators.length).toBe(2);
    })",79,85,steel
/src/collections/__tests__/VariableDeclarator-test.js,Magic Number,"{'line': 92, 'column': 38, 'index': 2654}","it('finds module imports (require) by module name', function() {
      const declarators = Collection.fromNodes(nodes)
        .findVariableDeclarators()
        .filter(VariableDeclaratorCollection.filters.requiresModule('module'));

      expect(declarators.length).toBe(1);
    })",87,93,steel
/src/collections/__tests__/VariableDeclarator-test.js,Magic Number,"{'line': 102, 'column': 38, 'index': 2961}","it('accepts multiple module names', function() {
      const declarators = Collection.fromNodes(nodes)
        .findVariableDeclarators()
        .filter(VariableDeclaratorCollection.filters.requiresModule(
          ['module', 'module2']
        ));

      expect(declarators.length).toBe(2);
    })",95,103,steel
/src/collections/__tests__/VariableDeclarator-test.js,Magic Number,"{'line': 117, 'column': 38, 'index': 3412}","it('renames variable declarations considering scope', function() {
      Collection.fromNodes(nodes)
        .findVariableDeclarators()
        .filter(VariableDeclaratorCollection.filters.requiresModule('module'))
        .renameTo('xyz');

      const identifiers =
        Collection.fromNodes(nodes)
        .find(types.Identifier, {name: 'xyz'});

      expect(identifiers.length).toBe(6);
    })",107,118,steel
/src/collections/__tests__/VariableDeclarator-test.js,Magic Number,"{'line': 129, 'column': 38, 'index': 3749}","it('does not rename things that are not variables', function() {
      Collection.fromNodes(nodes)
        .findVariableDeclarators('blah')
        .renameTo('blarg');

      const identifiers =
        Collection.fromNodes(nodes)
        .find(types.Identifier, {name: 'blarg'});

      expect(identifiers.length).toBe(1);
    })",120,130,steel
/src/collections/__tests__/VariableDeclarator-test.js,Magic Number,"{'line': 150, 'column': 13, 'index': 4335}","it('properly renames a shorthand property that was using the old variable name', function() {
      nodes = [recast.parse([
        'var foo = 42;',
        'var obj2 = {',
        '  foo,',
        '};',
      ].join('\n'), {parser: getParser()}).program];

      // Outputs:
      // var newFoo = 42;
      // var obj2 = {
      //   foo: newFoo,
      // };
      Collection.fromNodes(nodes)
        .findVariableDeclarators('foo').renameTo('newFoo');

      expect(
        Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
      ).toBe(2);
      expect(
        Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
      ).toBe(1);

      expect(
        Collection.fromNodes(nodes).find(types.Property).filter(prop => !prop.value.shorthand).length
      ).toBe(1);
      expect(
        Collection.fromNodes(nodes).find(types.Property).filter(prop => prop.value.shorthand).length
      ).toBe(0);
    })",132,161,steel
/src/collections/__tests__/VariableDeclarator-test.js,Magic Number,"{'line': 153, 'column': 13, 'index': 4449}","it('properly renames a shorthand property that was using the old variable name', function() {
      nodes = [recast.parse([
        'var foo = 42;',
        'var obj2 = {',
        '  foo,',
        '};',
      ].join('\n'), {parser: getParser()}).program];

      // Outputs:
      // var newFoo = 42;
      // var obj2 = {
      //   foo: newFoo,
      // };
      Collection.fromNodes(nodes)
        .findVariableDeclarators('foo').renameTo('newFoo');

      expect(
        Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
      ).toBe(2);
      expect(
        Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
      ).toBe(1);

      expect(
        Collection.fromNodes(nodes).find(types.Property).filter(prop => !prop.value.shorthand).length
      ).toBe(1);
      expect(
        Collection.fromNodes(nodes).find(types.Property).filter(prop => prop.value.shorthand).length
      ).toBe(0);
    })",132,161,steel
/src/collections/__tests__/VariableDeclarator-test.js,Magic Number,"{'line': 157, 'column': 13, 'index': 4583}","it('properly renames a shorthand property that was using the old variable name', function() {
      nodes = [recast.parse([
        'var foo = 42;',
        'var obj2 = {',
        '  foo,',
        '};',
      ].join('\n'), {parser: getParser()}).program];

      // Outputs:
      // var newFoo = 42;
      // var obj2 = {
      //   foo: newFoo,
      // };
      Collection.fromNodes(nodes)
        .findVariableDeclarators('foo').renameTo('newFoo');

      expect(
        Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
      ).toBe(2);
      expect(
        Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
      ).toBe(1);

      expect(
        Collection.fromNodes(nodes).find(types.Property).filter(prop => !prop.value.shorthand).length
      ).toBe(1);
      expect(
        Collection.fromNodes(nodes).find(types.Property).filter(prop => prop.value.shorthand).length
      ).toBe(0);
    })",132,161,steel
/src/collections/__tests__/VariableDeclarator-test.js,Magic Number,"{'line': 160, 'column': 13, 'index': 4715}","it('properly renames a shorthand property that was using the old variable name', function() {
      nodes = [recast.parse([
        'var foo = 42;',
        'var obj2 = {',
        '  foo,',
        '};',
      ].join('\n'), {parser: getParser()}).program];

      // Outputs:
      // var newFoo = 42;
      // var obj2 = {
      //   foo: newFoo,
      // };
      Collection.fromNodes(nodes)
        .findVariableDeclarators('foo').renameTo('newFoo');

      expect(
        Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
      ).toBe(2);
      expect(
        Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
      ).toBe(1);

      expect(
        Collection.fromNodes(nodes).find(types.Property).filter(prop => !prop.value.shorthand).length
      ).toBe(1);
      expect(
        Collection.fromNodes(nodes).find(types.Property).filter(prop => prop.value.shorthand).length
      ).toBe(0);
    })",132,161,steel
/src/collections/__tests__/VariableDeclarator-test.js,Magic Number,"{'line': 171, 'column': 38, 'index': 5041}","it('does not rename React component prop name', function () {
      Collection.fromNodes(nodes)
        .findVariableDeclarators('foo')
        .renameTo('xyz');

      const identifiers = Collection.fromNodes(nodes)
        .find(types.JSXIdentifier, { name: 'foo' });

      expect(identifiers.length).toBe(1);
    })",163,172,steel
/src/collections/__tests__/VariableDeclarator-test.js,Magic Number,"{'line': 187, 'column': 38, 'index': 5582}","it('does not rename JSX open and closing tags that start with a lowercase letter', function () {
      nodes = [recast.parse([
        'var span = useRef(null);',
        'var element = <span ref={span}></span>;',
      ].join('\n'), {parser: getParser()}).program];

      Collection.fromNodes(nodes)
        .findVariableDeclarators('span')
        .renameTo('spanRef');

      const identifiers = Collection.fromNodes(nodes)
        .find(types.JSXIdentifier, { name: 'spanRef' });

      expect(identifiers.length).toBe(0);
    })",174,188,steel
/src/collections/__tests__/VariableDeclarator-test.js,Magic Number,"{'line': 204, 'column': 38, 'index': 6158}","it('does rename JSX open and closing tags that are capitalized', function () {
      nodes = [recast.parse([
        'var Span = require(""./Span"");',
        'var span = useRef(null);',
        'var element = <Span ref={span}></Span>;',
      ].join('\n'), {parser: getParser()}).program];

      Collection.fromNodes(nodes)
        .findVariableDeclarators('Span')
        .renameTo('SpanComponent');

      const identifiers = Collection.fromNodes(nodes)
        .find(types.JSXIdentifier, { name: 'SpanComponent' });

      expect(identifiers.length).toBe(2);
    })",190,205,steel
/src/collections/__tests__/VariableDeclarator-test.js,Magic Number,"{'line': 220, 'column': 15, 'index': 6668}","it('does not rename object property', function () {
        nodes = [
          recast.parse('var foo = 42; var obj = { foo: null };', {
            parser: getParser('babylon'),
          }).program
        ];
        Collection
          .fromNodes(nodes)
          .findVariableDeclarators('foo').renameTo('newFoo');

        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
        ).toBe(1);
        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
        ).toBe(1);
      })",208,224,steel
/src/collections/__tests__/VariableDeclarator-test.js,Magic Number,"{'line': 223, 'column': 15, 'index': 6788}","it('does not rename object property', function () {
        nodes = [
          recast.parse('var foo = 42; var obj = { foo: null };', {
            parser: getParser('babylon'),
          }).program
        ];
        Collection
          .fromNodes(nodes)
          .findVariableDeclarators('foo').renameTo('newFoo');

        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
        ).toBe(1);
        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
        ).toBe(1);
      })",208,224,steel
/src/collections/__tests__/VariableDeclarator-test.js,Magic Number,"{'line': 238, 'column': 15, 'index': 7245}","it('does not rename object method', function () {
        nodes = [
          recast.parse('var foo = 42; var obj = { foo() {} };', {
            parser: getParser('babylon'),
          }).program
        ];
        Collection
          .fromNodes(nodes)
          .findVariableDeclarators('foo').renameTo('newFoo');

        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
        ).toBe(1);
        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
        ).toBe(1);
      })",226,242,steel
/src/collections/__tests__/VariableDeclarator-test.js,Magic Number,"{'line': 241, 'column': 15, 'index': 7365}","it('does not rename object method', function () {
        nodes = [
          recast.parse('var foo = 42; var obj = { foo() {} };', {
            parser: getParser('babylon'),
          }).program
        ];
        Collection
          .fromNodes(nodes)
          .findVariableDeclarators('foo').renameTo('newFoo');

        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
        ).toBe(1);
        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
        ).toBe(1);
      })",226,242,steel
/src/collections/__tests__/VariableDeclarator-test.js,Magic Number,"{'line': 256, 'column': 15, 'index': 7818}","it('does not rename class method', function () {
        nodes = [
          recast.parse('var foo = 42; class A { foo() {} }', {
            parser: getParser('babylon'),
          }).program
        ];
        Collection
          .fromNodes(nodes)
          .findVariableDeclarators('foo').renameTo('newFoo');

        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
        ).toBe(1);
        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
        ).toBe(1);
      })",244,260,steel
/src/collections/__tests__/VariableDeclarator-test.js,Magic Number,"{'line': 259, 'column': 15, 'index': 7938}","it('does not rename class method', function () {
        nodes = [
          recast.parse('var foo = 42; class A { foo() {} }', {
            parser: getParser('babylon'),
          }).program
        ];
        Collection
          .fromNodes(nodes)
          .findVariableDeclarators('foo').renameTo('newFoo');

        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'newFoo' }).length
        ).toBe(1);
        expect(
          Collection.fromNodes(nodes).find(types.Identifier, { name: 'foo' }).length
        ).toBe(1);
      })",244,260,steel
/src/collections/__tests__/Node-test.js,Magic Number,"{'line': 63, 'column': 33, 'index': 1524}","it('finds nodes by type', function() {
        const ast = b.sequenceExpression([ // eslint-disable-line no-shadow
          b.identifier('foo'),
          b.literal('asd'),
          b.identifier('bar'),
        ]);
        const vars = Collection.fromNodes([ast]).find(types.Identifier);

        expect(vars.length).toBe(2);
      })",55,64,steel
/src/collections/__tests__/Node-test.js,Magic Number,"{'line': 74, 'column': 33, 'index': 1845}","it('doesn\'t find the nodes in the collection itself', function() {
        const nodes = [
          b.identifier('foo'),
          b.literal('asd'),
          b.identifier('bar'),
        ];
        const vars = Collection.fromNodes(nodes).find(types.Identifier);

        expect(vars.length).toBe(0);
      })",66,75,steel
/src/collections/__tests__/Node-test.js,Magic Number,"{'line': 86, 'column': 33, 'index': 2231}","it('finds nodes by type and properties', function() {
        const ast = b.sequenceExpression([ // eslint-disable-line no-shadow
          b.identifier('foo'),
          b.literal('asd'),
          b.identifier('bar'),
        ]);
        const vars = Collection.fromNodes([ast])
          .find(types.Identifier, {name: 'bar'});

        expect(vars.length).toBe(1);
        expect(vars.nodes()[0]).toBe(ast.expressions[2]);
      })",77,88,steel
/src/collections/__tests__/Node-test.js,Magic Number,"{'line': 95, 'column': 33, 'index': 2555}","it('handles chained find calls properly', function() {
        const vars = Collection.fromNodes([ast])
          .find(types.FunctionDeclaration)
          .find(types.VariableDeclarator, {id: {name: 'bar'}});

        expect(vars.length).toBe(1);
        expect(vars.nodes()[0]).toBe(
          ast.body[1].body.body[0].declarations[0]
        );
      })",90,99,steel
/src/collections/__tests__/Node-test.js,Magic Number,"{'line': 111, 'column': 32, 'index': 3140}","it('handles multi chain find calls properly', function() {
        const functionBody = ast.body[1].body.body;
        const functionDeclarations = Collection.fromNodes([ast])
          .find(types.FunctionDeclaration);

        const bar = functionDeclarations
          .find(types.VariableDeclarator, {id: {name: 'bar'}});
        const baz = functionDeclarations
          .find(types.VariableDeclarator, {id: {name: 'baz'}});

        expect(bar.length).toBe(1);
        expect(bar.nodes()[0]).toBe(functionBody[0].declarations[0]);
        expect(baz.length).toBe(1);
        expect(baz.nodes()[0]).toBe(functionBody[1].declarations[0]);
      })",101,115,steel
/src/collections/__tests__/Node-test.js,Magic Number,"{'line': 113, 'column': 32, 'index': 3246}","it('handles multi chain find calls properly', function() {
        const functionBody = ast.body[1].body.body;
        const functionDeclarations = Collection.fromNodes([ast])
          .find(types.FunctionDeclaration);

        const bar = functionDeclarations
          .find(types.VariableDeclarator, {id: {name: 'bar'}});
        const baz = functionDeclarations
          .find(types.VariableDeclarator, {id: {name: 'baz'}});

        expect(bar.length).toBe(1);
        expect(bar.nodes()[0]).toBe(functionBody[0].declarations[0]);
        expect(baz.length).toBe(1);
        expect(baz.nodes()[0]).toBe(functionBody[1].declarations[0]);
      })",101,115,steel
/src/collections/__tests__/Node-test.js,Magic Number,"{'line': 156, 'column': 33, 'index': 4460}","it('finds closest node (up the tree) of the given type', function() {
        const functionDeclaration = ast.body[1];
        decl = Collection.fromNodes([ast])
          .find(types.Identifier)
          .closest(types.FunctionDeclaration);

        expect(decl.length).toBe(1);
        expect(decl.nodes()[0]).toBe(functionDeclaration);
      })",150,158,steel
/src/collections/__tests__/Node-test.js,Magic Number,"{'line': 163, 'column': 48, 'index': 4727}","it('allows to filter nodes by pattern', function() {
        const literals = Collection.fromNodes([decl])
          .find(types.Literal);
        expect(literals.get(0).node.value).toBe(3);
        const closest = literals.closest(
          types.FunctionDeclaration,
          {id: {name: 'foo'}}
        );
        expect(closest.get(0).node.id.name).toBe('foo');
      })",160,169,steel
/src/collections/__tests__/Node-test.js,Magic Number,"{'line': 174, 'column': 48, 'index': 5124}","it('allows to filter nodes with a filter function', function() {
        const literals = Collection.fromNodes([decl])
          .find(types.Literal);
        expect(literals.get(0).node.value).toBe(3);
        const closest = literals.closest(
          types.FunctionDeclaration,
          (node) => node.id && node.id.name === 'foo'
        );
        expect(closest.get(0).node.id.name).toBe('foo');
      })",171,180,steel
/src/collections/__tests__/Node-test.js,Magic Number,"{'line': 185, 'column': 48, 'index': 5536}","it('fails when filter evaluates as false', function() {
        const literals = Collection.fromNodes([decl])
          .find(types.Literal);
        expect(literals.get(0).node.value).toBe(3);
        const closest = literals.closest(
          types.FunctionDeclaration,
          (node) => node.id && node.id.name === 'blue'
        );
        expect(closest.nodes().length).toBe(0);
      })",182,191,steel
/src/collections/__tests__/Node-test.js,Magic Number,"{'line': 190, 'column': 44, 'index': 5729}","it('fails when filter evaluates as false', function() {
        const literals = Collection.fromNodes([decl])
          .find(types.Literal);
        expect(literals.get(0).node.value).toBe(3);
        const closest = literals.closest(
          types.FunctionDeclaration,
          (node) => node.id && node.id.name === 'blue'
        );
        expect(closest.nodes().length).toBe(0);
      })",182,191,steel
/src/collections/__tests__/Node-test.js,Magic Number,"{'line': 207, 'column': 33, 'index': 6368}","it('gets the variable declarators for each selected path', function() {
        const variableDeclarator =
          b.variableDeclarator(b.identifier('foo'), null);
        const program = b.program([
          b.variableDeclaration('var', [variableDeclarator]),
          b.expressionStatement(b.identifier('foo')),
          b.expressionStatement(b.identifier('bar'))
        ]);

        const decl = Collection.fromNodes([program])
          .find(types.Identifier)
          .getVariableDeclarators(p => p.value.name);
        expect(decl.length).toBe(1);
        expect(decl.nodes()[0]).toBe(variableDeclarator);
      })",195,209,steel
/src/collections/__tests__/Node-test.js,Magic Number,"{'line': 268, 'column': 55, 'index': 8420}","it('accepts a function as replacement ', function() {
        const ast = b.sequenceExpression([ // eslint-disable-line no-shadow
          b.identifier('foo'),
          b.literal('asd'),
          b.identifier('bar'),
        ]);

        const expectedArgs = [b.identifier('foo'), b.identifier('bar')];
        const receivedArgs = [];
        const replaceFunction =
          jest.fn(function(path, i) {
            // We have to keep a reference to the argument before it gets
            // replaced
            receivedArgs.push(path.value);
            return b.identifier(path.value.name + i);
          });

        const S = Collection.fromNodes([ast]);
        S.find(types.Identifier)
         .replaceWith(replaceFunction);

        expect(replaceFunction.mock.calls.length).toBe(2);
        expect(receivedArgs).toEqual(expectedArgs);
        // baz is properly replaced
        expect(S.nodes()[0].expressions[0]).toEqual(b.identifier('foo0'));
        // baz1 is properly replaced
        expect(S.nodes()[0].expressions[2]).toEqual(b.identifier('bar1'));
      })",247,274,steel
/src/collections/__tests__/Node-test.js,Magic Number,"{'line': 289, 'column': 45, 'index': 9215}","it('inserts a new node before the current one', function() {
        const ast = b.variableDeclaration( // eslint-disable-line no-shadow
          'var',
          [b.variableDeclarator(b.identifier('foo'), null)]
        );
        const one = b.variableDeclarator(b.identifier('one'), null);

        Collection.fromNodes([ast])
          .find(types.VariableDeclarator)
          .insertBefore(one);

        expect(ast.declarations.length).toBe(2);
        expect(ast.declarations[0]).toBe(one);
      })",278,291,steel
/src/collections/__tests__/Node-test.js,Magic Number,"{'line': 305, 'column': 45, 'index': 9792}","it('accepts an array of nodes', function() {
        const ast = b.variableDeclaration( // eslint-disable-line no-shadow
          'var',
          [b.variableDeclarator(b.identifier('foo'), null)]
        );
        const one = b.variableDeclarator(b.identifier('one'), null);
        const two = b.variableDeclarator(b.identifier('two'), null);

        Collection.fromNodes([ast])
          .find(types.VariableDeclarator)
          .insertBefore([one, two]);

        expect(ast.declarations.length).toBe(3);
        expect(ast.declarations[0]).toBe(one);
        expect(ast.declarations[1]).toBe(two);
      })",293,308,steel
/src/collections/__tests__/Node-test.js,Magic Number,"{'line': 322, 'column': 44, 'index': 10340}","it('accepts a function', function() {
        const x = b.identifier('x');
        const foo = b.identifier('foo');
        const bar = b.identifier('bar');
        const ast = b.sequenceExpression([foo, bar]); // eslint-disable-line no-shadow

        Collection.fromNodes([ast])
          .find(types.Identifier)
          .insertBefore(function() {
            return x;
          });

        expect(ast.expressions.length).toBe(4);
        expect(ast.expressions).toEqual([x, foo, x, bar]);
        expect(ast.expressions[0]).toBe(x);
        expect(ast.expressions[2]).toBe(x);
      })",310,326,steel
/src/collections/__tests__/Node-test.js,Magic Number,"{'line': 341, 'column': 45, 'index': 11004}","it('inserts a new node after the current one', function() {
        const ast = b.variableDeclaration( // eslint-disable-line no-shadow
          'var',
          [b.variableDeclarator(b.identifier('foo'), null)]
        );
        const one = b.variableDeclarator(b.identifier('one'), null);

        Collection.fromNodes([ast])
          .find(types.VariableDeclarator)
          .insertAfter(one);

        expect(ast.declarations.length).toBe(2);
        expect(ast.declarations[1]).toBe(one);
      })",330,343,steel
/src/collections/__tests__/Node-test.js,Magic Number,"{'line': 357, 'column': 45, 'index': 11580}","it('accepts an array of nodes', function() {
        const ast = b.variableDeclaration( // eslint-disable-line no-shadow
          'var',
          [b.variableDeclarator(b.identifier('foo'), null)]
        );
        const one = b.variableDeclarator(b.identifier('one'), null);
        const two = b.variableDeclarator(b.identifier('two'), null);

        Collection.fromNodes([ast])
          .find(types.VariableDeclarator)
          .insertAfter([one, two]);

        expect(ast.declarations.length).toBe(3);
        expect(ast.declarations[1]).toBe(one);
        expect(ast.declarations[2]).toBe(two);
      })",345,360,steel
/src/collections/__tests__/Node-test.js,Magic Number,"{'line': 374, 'column': 44, 'index': 12127}","it('accepts a function', function() {
        const x = b.identifier('x');
        const foo = b.identifier('foo');
        const bar = b.identifier('bar');
        const ast = b.sequenceExpression([foo, bar]); // eslint-disable-line no-shadow

        Collection.fromNodes([ast])
          .find(types.Identifier)
          .insertAfter(function() {
            return x;
          });

        expect(ast.expressions.length).toBe(4);
        expect(ast.expressions).toEqual([foo, x, bar, x]);
        expect(ast.expressions[1]).toBe(x);
        expect(ast.expressions[3]).toBe(x);
      })",362,378,steel
/src/collections/__tests__/Node-test.js,Magic Number,"{'line': 391, 'column': 37, 'index': 12750}","it('removes a node if it is part of the body of a statement', function() {
        const x = b.expressionStatement(b.identifier('x'));
        const y = b.expressionStatement(b.identifier('y'));
        const ast = b.program([x, y]); // eslint-disable-line no-shadow

        Collection.fromNodes([ast])
          .find(types.Identifier, {name: 'x'})
          .remove();

        expect(ast.body.length).toBe(1);
        expect(ast.body[0]).toBe(y);
      })",382,393,steel
/src/collections/__tests__/Node-test.js,Magic Number,"{'line': 406, 'column': 39, 'index': 13223}","it('removes a node if it is a function param', function() {
        const x = b.identifier('x');
        const y = b.identifier('y');
        const ast = b.arrowFunctionExpression( // eslint-disable-line no-shadow
          [x, b.identifier('z')],
          y
        );

        Collection.fromNodes([ast])
          .find(types.Identifier, {name: 'z'})
          .remove();
        expect(ast.params.length).toBe(1);
        expect(ast.params[0]).toBe(x);
        expect(ast.body).toBe(y);
      })",395,409,steel
/src/collections/__tests__/JSXElement-test.js,Magic Number,"{'line': 49, 'column': 41, 'index': 1293}","it('returns a non empty JSXCollection', function() {
      const jsx = Collection.fromNodes(nodes).find(types.JSXElement);
      expect(jsx.getTypes()).toContain('JSXElement');
      expect(jsx.length).toBeGreaterThan(0);
    })",46,50,steel
/src/collections/__tests__/JSXElement-test.js,Magic Number,"{'line': 55, 'column': 30, 'index': 1478}","it('lets us find JSXElements by name conveniently', function() {
      const jsx = Collection.fromNodes(nodes).findJSXElements('Child');

      expect(jsx.length).toBe(3);
    })",52,56,steel
/src/collections/__tests__/JSXElement-test.js,Magic Number,"{'line': 61, 'column': 30, 'index': 1660}","it('finds JSXElements by module name', function() {
      const jsx = Collection.fromNodes(nodes).findJSXElementsByModuleName('XYZ');

      expect(jsx.length).toBe(1);
    })",58,62,steel
/src/collections/__tests__/JSXElement-test.js,Magic Number,"{'line': 69, 'column': 35, 'index': 1889}","it('returns the child nodes of an JSXElement', function() {
      const children =
        Collection.fromNodes(nodes)
        .findJSXElements('FooBar')
        .childNodes();
      expect(children.length).toBe(7);
      expect(children.getTypes()).toContain('Expression');
    })",64,71,steel
/src/collections/__tests__/JSXElement-test.js,Magic Number,"{'line': 79, 'column': 35, 'index': 2187}","it('returns the child JSXElements of an JSXElement', function() {
      const children =
        Collection.fromNodes(nodes)
        .findJSXElements('FooBar')
        .childElements();

      expect(children.length).toBe(2);
      expect(children.getTypes()).toContain('JSXElement');
    })",73,81,steel
/src/collections/__tests__/JSXElement-test.js,Magic Number,"{'line': 89, 'column': 35, 'index': 2518}","it('returns the child element types of an JSXElement', function() {
      const children =
        Collection.fromNodes(nodes)
        .findJSXElements('FooBar')
        .childNodesOfType(types.JSXExpressionContainer);

      expect(children.length).toBe(1);
      expect(children.getTypes()).toContain('JSXExpressionContainer');
    })",83,91,steel
/src/collections/__tests__/JSXElement-test.js,Magic Number,"{'line': 99, 'column': 35, 'index': 2825}","it('returns a properly typed collection even if empty', function() {
      const children =
        Collection.fromNodes([])
        .findJSXElements('Foo')
        .childElements();

      expect(children.length).toBe(0);
      expect(children.getTypes()).toContain('JSXElement');
    })",93,101,steel
/src/collections/__tests__/JSXElement-test.js,Magic Number,"{'line': 109, 'column': 30, 'index': 3172}","it('filters elements by attributes', function() {
      const jsx = Collection.fromNodes(nodes)
        .findJSXElements()
        .filter(JSXElementCollection.filters.hasAttributes({foo: 'bar'}));
      expect(jsx.length).toBe(2);
    })",105,110,steel
/src/collections/__tests__/JSXElement-test.js,Magic Number,"{'line': 121, 'column': 30, 'index': 3550}","it('accepts callback functions as attribute filters', function() {
      const jsx = Collection.fromNodes(nodes)
        .findJSXElements()
        .filter(JSXElementCollection.filters.hasAttributes(
            {
              foo: v => ['bar', 'baz'].indexOf(v) > -1,
              baz: v => v === null
            }
        ));
      expect(jsx.length).toBe(2);
    })",112,122,steel
/src/collections/__tests__/JSXElement-test.js,Magic Number,"{'line': 128, 'column': 30, 'index': 3786}","it('filters elements by children', function() {
      const jsx = Collection.fromNodes(nodes)
        .findJSXElements()
        .filter(JSXElementCollection.filters.hasChildren('Child'));
      expect(jsx.length).toBe(2);
    })",124,129,steel
/src/collections/__tests__/JSXElement-test.js,Magic Number,"{'line': 162, 'column': 39, 'index': 4944}","it('handles insertions before children correctly', function() {
      const childElement = b.jsxElement(
        b.jsxOpeningElement(b.jsxIdentifier('Bar'), [], true)
      );
      const newChildElement = b.jsxElement(
        b.jsxOpeningElement(b.jsxIdentifier('Baz'), [], true)
      );
      const literal = b.literal('\n  ');
      const ast = b.jsxElement(
        b.jsxOpeningElement(b.jsxIdentifier('Foo')),
        b.jsxClosingElement(b.jsxIdentifier('Foo')),
        [literal, childElement, literal, childElement, b.literal('\n')]
      );

      Collection.fromNodes([ast])
        .childElements().at(1).insertBefore(newChildElement);

      expect(ast.children.length).toBe(6);
      expect(ast.children[3]).toBe(newChildElement);
    })",145,164,steel
/src/collections/__tests__/ImportDeclaration-test.js,Duplicate Assert,"{'line': 84, 'column': 10, 'index': 2402}","it(""renames an ImportDeclaration with the given sourcePath"", function () {
        Collection.fromNodes(nodes).renameImportDeclaration(""XYZ"", ""ABC"");

        {
          const imports =
            Collection.fromNodes(nodes).findImportDeclarations(""ABC"");

          expect(imports.length).toBe(1);
        }
        {
          const imports =
            Collection.fromNodes(nodes).findImportDeclarations(""XYZ"");
          expect(imports.length).toBe(0);
        }
      })",77,91,steel
/src/collections/__tests__/ImportDeclaration-test.js,Duplicate Assert,"{'line': 89, 'column': 10, 'index': 2561}","it(""renames an ImportDeclaration with the given sourcePath"", function () {
        Collection.fromNodes(nodes).renameImportDeclaration(""XYZ"", ""ABC"");

        {
          const imports =
            Collection.fromNodes(nodes).findImportDeclarations(""ABC"");

          expect(imports.length).toBe(1);
        }
        {
          const imports =
            Collection.fromNodes(nodes).findImportDeclarations(""XYZ"");
          expect(imports.length).toBe(0);
        }
      })",77,91,steel
/src/collections/__tests__/ImportDeclaration-test.js,Magic Number,"{'line': 65, 'column': 36, 'index': 1799}","it(""lets us find ImportDeclarations by source path conveniently"", function () {
        const imports =
          Collection.fromNodes(nodes).findImportDeclarations(""XYZ"");

        expect(imports.length).toBe(1);
      })",61,66,steel
/src/collections/__tests__/ImportDeclaration-test.js,Magic Number,"{'line': 72, 'column': 36, 'index': 2050}","it(""returns an empty ImportDeclarationCollection if no ImportDeclarations are found"", function () {
        const imports =
          Collection.fromNodes(nodes).findImportDeclarations(""Foo"");

        expect(imports.length).toBe(0);
      })",68,73,steel
/src/collections/__tests__/ImportDeclaration-test.js,Magic Number,"{'line': 84, 'column': 38, 'index': 2430}","it(""renames an ImportDeclaration with the given sourcePath"", function () {
        Collection.fromNodes(nodes).renameImportDeclaration(""XYZ"", ""ABC"");

        {
          const imports =
            Collection.fromNodes(nodes).findImportDeclarations(""ABC"");

          expect(imports.length).toBe(1);
        }
        {
          const imports =
            Collection.fromNodes(nodes).findImportDeclarations(""XYZ"");
          expect(imports.length).toBe(0);
        }
      })",77,91,steel
/src/collections/__tests__/ImportDeclaration-test.js,Magic Number,"{'line': 89, 'column': 38, 'index': 2589}","it(""renames an ImportDeclaration with the given sourcePath"", function () {
        Collection.fromNodes(nodes).renameImportDeclaration(""XYZ"", ""ABC"");

        {
          const imports =
            Collection.fromNodes(nodes).findImportDeclarations(""ABC"");

          expect(imports.length).toBe(1);
        }
        {
          const imports =
            Collection.fromNodes(nodes).findImportDeclarations(""XYZ"");
          expect(imports.length).toBe(0);
        }
      })",77,91,steel
/src/collections/__tests__/ImportDeclaration-test.js,Magic Number,"{'line': 117, 'column': 36, 'index': 3511}","it(""inserts an ImportDeclaration into the AST"", function () {
        Collection.fromNodes(nodes).insertImportDeclaration(""@foo/bar"", [
          b.importDefaultSpecifier(b.identifier(""Foo"")),
          b.importSpecifier(b.identifier(""ABC"")),
          b.importSpecifier(b.identifier(""123"")),
        ]);

        const imports =
          Collection.fromNodes(nodes).findImportDeclarations(""@foo/bar"");

        expect(imports.length).toBe(1);

        const importSpecifiers = imports.paths()[0].value.specifiers;
        expect(importSpecifiers.length).toBe(3);
      })",107,121,steel
/src/collections/__tests__/ImportDeclaration-test.js,Magic Number,"{'line': 120, 'column': 45, 'index': 3631}","it(""inserts an ImportDeclaration into the AST"", function () {
        Collection.fromNodes(nodes).insertImportDeclaration(""@foo/bar"", [
          b.importDefaultSpecifier(b.identifier(""Foo"")),
          b.importSpecifier(b.identifier(""ABC"")),
          b.importSpecifier(b.identifier(""123"")),
        ]);

        const imports =
          Collection.fromNodes(nodes).findImportDeclarations(""@foo/bar"");

        expect(imports.length).toBe(1);

        const importSpecifiers = imports.paths()[0].value.specifiers;
        expect(importSpecifiers.length).toBe(3);
      })",107,121,steel
/src/collections/__tests__/ImportDeclaration-test.js,Magic Number,"{'line': 137, 'column': 36, 'index': 4240}","it(""does not insert duplicate ImportDeclarations"", function () {
        Collection.fromNodes(nodes).insertImportDeclaration(""@foo/baz"", [
          b.importDefaultSpecifier(b.identifier(""Foo"")),
          b.importSpecifier(b.identifier(""ABC"")),
        ]);

        Collection.fromNodes(nodes).insertImportDeclaration(""@foo/baz"", [
          b.importDefaultSpecifier(b.identifier(""Foo"")),
          b.importSpecifier(b.identifier(""ABC"")),
        ]);

        const imports =
          Collection.fromNodes(nodes).findImportDeclarations(""@foo/baz"");

        expect(imports.length).toBe(1);
      })",123,138,steel
/src/__tests__/testUtils-test.js,Duplicate Assert,"{'line': 35, 'column': 6, 'index': 1151}","it('should apply transformation', () => {
      const moduleToTransform = getModuleToTransform();
      const transformedCode = testUtils.applyTransform(testSyncTransform, null, moduleToTransform);

      expect(transformedCode).not.toMatch(/sum/);
      expect(transformedCode).toMatch(/addition/);
    })",31,37,steel
/src/__tests__/testUtils-test.js,Duplicate Assert,"{'line': 36, 'column': 6, 'index': 1201}","it('should apply transformation', () => {
      const moduleToTransform = getModuleToTransform();
      const transformedCode = testUtils.applyTransform(testSyncTransform, null, moduleToTransform);

      expect(transformedCode).not.toMatch(/sum/);
      expect(transformedCode).toMatch(/addition/);
    })",31,37,steel
/src/__tests__/testUtils-test.js,Duplicate Assert,"{'line': 95, 'column': 6, 'index': 2765}","it('should apply transformation', async () => {
      const moduleToTransform = getModuleToTransform();
      const transformedCode = await testUtils.applyTransform(testAsyncTransform, null, moduleToTransform);

      expect(transformedCode).not.toMatch(/sum/);
      expect(transformedCode).toMatch(/addition/);
    })",91,97,steel
/src/__tests__/testUtils-test.js,Duplicate Assert,"{'line': 96, 'column': 6, 'index': 2815}","it('should apply transformation', async () => {
      const moduleToTransform = getModuleToTransform();
      const transformedCode = await testUtils.applyTransform(testAsyncTransform, null, moduleToTransform);

      expect(transformedCode).not.toMatch(/sum/);
      expect(transformedCode).toMatch(/addition/);
    })",91,97,steel
/src/__tests__/testUtils-test.js,Unknown Test,"{'column': 4, 'line': 39}","it('should run test', () => {
      testUtils.runTest(
        __dirname,
        path.join('__testfixtures__',
        'test-sync-transform'),
        null,
        'test-sync-transform'
      );
    })",39,47,steel
/src/__tests__/testUtils-test.js,Unknown Test,"{'column': 4, 'line': 49}","it ('should run snapshot test', () => {
      const moduleToTransform = getModuleToTransform();
      testUtils.runSnapshotTest(testSyncTransform, null, moduleToTransform);
    })",49,52,steel
/src/__tests__/testUtils-test.js,Unknown Test,"{'column': 4, 'line': 54}","it('should run inline test', () => {
      const moduleToTransform = getModuleToTransform();
      testUtils.runInlineTest(testSyncTransform, null, moduleToTransform, expectedInlineOutput);
    })",54,57,steel
/src/__tests__/testUtils-test.js,Unknown Test,"{'column': 4, 'line': 99}","it('should run test', () => {
      return testUtils.runTest(__dirname, path.join('__testfixtures__', 'test-async-transform'), null, 'test-async-transform');
    })",99,101,steel
/src/__tests__/testUtils-test.js,Unknown Test,"{'column': 4, 'line': 103}","it ('should run snapshot test', () => {
      const moduleToTransform = getModuleToTransform();
      return testUtils.runSnapshotTest(testAsyncTransform, null, moduleToTransform);
    })",103,106,steel
/src/__tests__/testUtils-test.js,Unknown Test,"{'column': 4, 'line': 108}","it('should run inline test', () => {
      const moduleToTransform = getModuleToTransform();
      return testUtils.runInlineTest(testAsyncTransform, null, moduleToTransform, expectedInlineOutput);
    })",108,111,steel
/src/__tests__/template-test.js,Conditional Test Logic,"{'line': 123, 'column': 2, 'index': 2612}","describe('Templates', () => {
  let statements;
  let statement;
  let expression;
  let jscodeshift;

  beforeEach(() => {
    jest.resetModules();

    jscodeshift = require('../core');
    const template = jscodeshift.template;
    expression = template.expression;
    statement = template.statement;
    statements = template.statements;
  });

  it('interpolates expression nodes with source code', () => {

    let input =
`var foo = bar;
if(bar) {
  console.log(42);
}`;

    let expected =
`var foo = alert(bar);
if(alert(bar)) {
  console.log(42);
}`;

    expect(
      jscodeshift(input)
        .find('Identifier', {name: 'bar'})
        .replaceWith(path => expression`alert(${path.node})`)
        .toSource()
    ).toEqual(expected);
  });

  it('interpolates statement nodes with source code', () => {
    let input =
`for (var i = 0; i < 10; i++) {
  console.log(i);
  console.log(i / 2);
}`;

    let expected =
`var i = 0;
while (i < 10) {
  console.log(i);
  console.log(i / 2);
  i++;
}`;

    expect(
      jscodeshift(input)
        .find('ForStatement')
        .replaceWith(
          p => statements`
            ${p.node.init};
            while (${p.node.test}) {
              ${p.node.body.body}
              ${p.node.update};
            }`
        )
        .toSource()
    ).toEqual(expected);
  });

  it('can be used with a different parser', () => {
    const parser = require('../../parser/flow')();
    const template = require('../template')(parser);
    const node = {type: 'Literal', value: 41};

    expect(
      jscodeshift(template.expression`1 + ${node}`, {parser}).toSource()
    ).toEqual('1 + 41');
  });

  it('handles out-of-order traversal', () => {
    const input = 'var x';
    const expected = 'class X extends a {f(b) {}}';

    const a = jscodeshift.identifier('a');
    const b = jscodeshift.identifier('b');

    const classDecl = statement`
      class X extends ${a} {f(${b}) {}}
    `;

    expect(
      jscodeshift(input)
        .find('VariableDeclaration')
        .replaceWith(classDecl)
        .toSource()
    )
    .toEqual(expected);
  });
  it('correctly parses expressions without any interpolation', () => {
    const expected = 'function() {}';

    expect(
      jscodeshift(
        expression`function() {}`
      )
      .toSource()
    )
    .toEqual(expected);
  });

  for (const parser of ['babel', 'babylon', 'flow', 'ts', 'tsx']) {
    it(`asyncExpression correctly parses expressions with await -- ${parser}`, () => {
      const expected = '{\n  bar: await baz\n}'
      const j = jscodeshift.withParser(parser)

      expect(j(j.template.asyncExpression`{\n  bar: await baz\n}`).toSource()).toEqual(expected)
    })
  }

  describe('explode arrays', () => {

    it('explodes arrays in function definitions', () => {
      let input = 'var foo = [a, b];';
      let expected = 'var foo = function foo(a, b, c) {};';

      expect(
        jscodeshift(input)
          .find('ArrayExpression')
          .replaceWith(
            p => expression`function foo(${p.node.elements}, c) {}`
          )
          .toSource()
      )
      .toEqual(expected);

      expected = 'var foo = function(a, b, c) {};';

      expect(
        jscodeshift(input)
          .find('ArrayExpression')
          .replaceWith(
            p => expression`function(${p.node.elements}, c) {}`
          )
          .toSource()
      )
      .toEqual(expected);

      expected = 'var foo = (a, b) => {};';

      expect(
        jscodeshift(input)
          .find('ArrayExpression')
          .replaceWith(
            p => expression`${p.node.elements} => {}`
          )
          .toSource()
      )
      .toEqual(expected);

      expected = 'var foo = (a, b, c) => {};';

      expect(
        jscodeshift(input)
          .find('ArrayExpression')
          .replaceWith(
            p => expression`(${p.node.elements}, c) => {}`
          )
          .toSource()
      )
      .toEqual(expected);
    });

    it('explodes arrays in variable declarations', () => {
      let input = 'var foo = [a, b];';
      let expected = 'var foo, a, b;';
      expect(
        jscodeshift(input)
          .find('VariableDeclaration')
          // Need to use a block here because the arrow doesn't seem to be
          // compiled with a line break after the return statement. Can't repro
          // outside here though
          .replaceWith(p => {
            const node = p.node.declarations[0];
            return statement`var ${node.id}, ${node.init.elements};`;
          })
          .toSource()
      )
      .toEqual(expected);
    });

    it('explodes arrays in array expressions', () => {
      let input = 'var foo = [a, b];';
      let expected = 'var foo = [a, b, c];';
      expect(
        jscodeshift(input)
          .find('ArrayExpression')
          .replaceWith(p => expression`[${p.node.elements}, c]`)
          .toSource()
      )
      .toEqual(expected);
    });

    it('explodes arrays in object expressions', () => {
      let input = 'var foo = {a, b};';
      let expected = /var foo = \{\s*a,\s*b,\s*c: 42\s*};/;
      expect(
        jscodeshift(input)
          .find('ObjectExpression')
          .replaceWith(p => expression`{${p.node.properties}, c: 42}`)
          .toSource()
      )
      .toMatch(expected);
    });

    it('explodes arrays in call expressions', () => {
      let input = 'var foo = [a, b];';
      let expected = 'var foo = bar(a, b, c);';

      expect(
        jscodeshift(input)
          .find('ArrayExpression')
          .replaceWith(
            p => expression`bar(${p.node.elements}, c)`
          )
          .toSource()
      )
      .toEqual(expected);
    });

  });

})",14,244,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 26, 'column': 4, 'index': 604}","it('matches null and undefined', function() {
    expect(null).toMatchNode(null);
    expect(null).not.toMatchNode(undefined);

    expect(undefined).toMatchNode(undefined);
    expect(undefined).not.toMatchNode(null);
  })",25,31,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 27, 'column': 4, 'index': 640}","it('matches null and undefined', function() {
    expect(null).toMatchNode(null);
    expect(null).not.toMatchNode(undefined);

    expect(undefined).toMatchNode(undefined);
    expect(undefined).not.toMatchNode(null);
  })",25,31,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 29, 'column': 4, 'index': 686}","it('matches null and undefined', function() {
    expect(null).toMatchNode(null);
    expect(null).not.toMatchNode(undefined);

    expect(undefined).toMatchNode(undefined);
    expect(undefined).not.toMatchNode(null);
  })",25,31,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 30, 'column': 4, 'index': 732}","it('matches null and undefined', function() {
    expect(null).toMatchNode(null);
    expect(null).not.toMatchNode(undefined);

    expect(undefined).toMatchNode(undefined);
    expect(undefined).not.toMatchNode(null);
  })",25,31,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 34, 'column': 4, 'index': 821}","it('matches scalars', function() {
    expect('foo').toMatchNode('foo');
    expect('foo').not.toMatchNode('bar');
    expect('123').not.toMatchNode(123);

    expect(123).toMatchNode(123);
    expect(123).not.toMatchNode(456);
    expect(123).not.toMatchNode('123');

    expect(true).toMatchNode(true);
    expect(true).not.toMatchNode(false);
    expect(true).not.toMatchNode('true');
  })",33,45,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 35, 'column': 4, 'index': 859}","it('matches scalars', function() {
    expect('foo').toMatchNode('foo');
    expect('foo').not.toMatchNode('bar');
    expect('123').not.toMatchNode(123);

    expect(123).toMatchNode(123);
    expect(123).not.toMatchNode(456);
    expect(123).not.toMatchNode('123');

    expect(true).toMatchNode(true);
    expect(true).not.toMatchNode(false);
    expect(true).not.toMatchNode('true');
  })",33,45,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 38, 'column': 4, 'index': 942}","it('matches scalars', function() {
    expect('foo').toMatchNode('foo');
    expect('foo').not.toMatchNode('bar');
    expect('123').not.toMatchNode(123);

    expect(123).toMatchNode(123);
    expect(123).not.toMatchNode(456);
    expect(123).not.toMatchNode('123');

    expect(true).toMatchNode(true);
    expect(true).not.toMatchNode(false);
    expect(true).not.toMatchNode('true');
  })",33,45,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 39, 'column': 4, 'index': 976}","it('matches scalars', function() {
    expect('foo').toMatchNode('foo');
    expect('foo').not.toMatchNode('bar');
    expect('123').not.toMatchNode(123);

    expect(123).toMatchNode(123);
    expect(123).not.toMatchNode(456);
    expect(123).not.toMatchNode('123');

    expect(true).toMatchNode(true);
    expect(true).not.toMatchNode(false);
    expect(true).not.toMatchNode('true');
  })",33,45,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 40, 'column': 4, 'index': 1014}","it('matches scalars', function() {
    expect('foo').toMatchNode('foo');
    expect('foo').not.toMatchNode('bar');
    expect('123').not.toMatchNode(123);

    expect(123).toMatchNode(123);
    expect(123).not.toMatchNode(456);
    expect(123).not.toMatchNode('123');

    expect(true).toMatchNode(true);
    expect(true).not.toMatchNode(false);
    expect(true).not.toMatchNode('true');
  })",33,45,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 42, 'column': 4, 'index': 1055}","it('matches scalars', function() {
    expect('foo').toMatchNode('foo');
    expect('foo').not.toMatchNode('bar');
    expect('123').not.toMatchNode(123);

    expect(123).toMatchNode(123);
    expect(123).not.toMatchNode(456);
    expect(123).not.toMatchNode('123');

    expect(true).toMatchNode(true);
    expect(true).not.toMatchNode(false);
    expect(true).not.toMatchNode('true');
  })",33,45,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 43, 'column': 4, 'index': 1091}","it('matches scalars', function() {
    expect('foo').toMatchNode('foo');
    expect('foo').not.toMatchNode('bar');
    expect('123').not.toMatchNode(123);

    expect(123).toMatchNode(123);
    expect(123).not.toMatchNode(456);
    expect(123).not.toMatchNode('123');

    expect(true).toMatchNode(true);
    expect(true).not.toMatchNode(false);
    expect(true).not.toMatchNode('true');
  })",33,45,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 44, 'column': 4, 'index': 1132}","it('matches scalars', function() {
    expect('foo').toMatchNode('foo');
    expect('foo').not.toMatchNode('bar');
    expect('123').not.toMatchNode(123);

    expect(123).toMatchNode(123);
    expect(123).not.toMatchNode(456);
    expect(123).not.toMatchNode('123');

    expect(true).toMatchNode(true);
    expect(true).not.toMatchNode(false);
    expect(true).not.toMatchNode('true');
  })",33,45,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 48, 'column': 4, 'index': 1217}","it('matches arrays', function() {
    expect([1, 2, 3]).toMatchNode([1, 2, 3]);
    expect([1, 2, 3]).not.toMatchNode([4, 5, 6]);

    expect([[1, 2, 3], 'foo']).toMatchNode([[1, 2, 3], 'foo']);
    expect([[1, 2, 3], 'foo']).not.toMatchNode([[456], 'foo']);
    expect([[1, 2, 3], 'foo']).not.toMatchNode([[1, 2, 3], 'bar']);

    expect([1, 2, 3, 4]).toMatchNode([1, 2, 3]);
    expect([1, 2, 3]).not.toMatchNode([1, 2, 3, 4]);
  })",47,57,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 49, 'column': 4, 'index': 1263}","it('matches arrays', function() {
    expect([1, 2, 3]).toMatchNode([1, 2, 3]);
    expect([1, 2, 3]).not.toMatchNode([4, 5, 6]);

    expect([[1, 2, 3], 'foo']).toMatchNode([[1, 2, 3], 'foo']);
    expect([[1, 2, 3], 'foo']).not.toMatchNode([[456], 'foo']);
    expect([[1, 2, 3], 'foo']).not.toMatchNode([[1, 2, 3], 'bar']);

    expect([1, 2, 3, 4]).toMatchNode([1, 2, 3]);
    expect([1, 2, 3]).not.toMatchNode([1, 2, 3, 4]);
  })",47,57,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 51, 'column': 4, 'index': 1314}","it('matches arrays', function() {
    expect([1, 2, 3]).toMatchNode([1, 2, 3]);
    expect([1, 2, 3]).not.toMatchNode([4, 5, 6]);

    expect([[1, 2, 3], 'foo']).toMatchNode([[1, 2, 3], 'foo']);
    expect([[1, 2, 3], 'foo']).not.toMatchNode([[456], 'foo']);
    expect([[1, 2, 3], 'foo']).not.toMatchNode([[1, 2, 3], 'bar']);

    expect([1, 2, 3, 4]).toMatchNode([1, 2, 3]);
    expect([1, 2, 3]).not.toMatchNode([1, 2, 3, 4]);
  })",47,57,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 52, 'column': 4, 'index': 1378}","it('matches arrays', function() {
    expect([1, 2, 3]).toMatchNode([1, 2, 3]);
    expect([1, 2, 3]).not.toMatchNode([4, 5, 6]);

    expect([[1, 2, 3], 'foo']).toMatchNode([[1, 2, 3], 'foo']);
    expect([[1, 2, 3], 'foo']).not.toMatchNode([[456], 'foo']);
    expect([[1, 2, 3], 'foo']).not.toMatchNode([[1, 2, 3], 'bar']);

    expect([1, 2, 3, 4]).toMatchNode([1, 2, 3]);
    expect([1, 2, 3]).not.toMatchNode([1, 2, 3, 4]);
  })",47,57,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 53, 'column': 4, 'index': 1442}","it('matches arrays', function() {
    expect([1, 2, 3]).toMatchNode([1, 2, 3]);
    expect([1, 2, 3]).not.toMatchNode([4, 5, 6]);

    expect([[1, 2, 3], 'foo']).toMatchNode([[1, 2, 3], 'foo']);
    expect([[1, 2, 3], 'foo']).not.toMatchNode([[456], 'foo']);
    expect([[1, 2, 3], 'foo']).not.toMatchNode([[1, 2, 3], 'bar']);

    expect([1, 2, 3, 4]).toMatchNode([1, 2, 3]);
    expect([1, 2, 3]).not.toMatchNode([1, 2, 3, 4]);
  })",47,57,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 56, 'column': 4, 'index': 1560}","it('matches arrays', function() {
    expect([1, 2, 3]).toMatchNode([1, 2, 3]);
    expect([1, 2, 3]).not.toMatchNode([4, 5, 6]);

    expect([[1, 2, 3], 'foo']).toMatchNode([[1, 2, 3], 'foo']);
    expect([[1, 2, 3], 'foo']).not.toMatchNode([[456], 'foo']);
    expect([[1, 2, 3], 'foo']).not.toMatchNode([[1, 2, 3], 'bar']);

    expect([1, 2, 3, 4]).toMatchNode([1, 2, 3]);
    expect([1, 2, 3]).not.toMatchNode([1, 2, 3, 4]);
  })",47,57,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 60, 'column': 4, 'index': 1657}","it('matches objects', function() {
    expect({}).toMatchNode({});
    expect({name: 'foo'}).toMatchNode({name: 'foo'});
    expect({name: 'foo'}).not.toMatchNode({name: 'bar'});

    expect({name: 'foo', value: {name: 'bar'}})
      .toMatchNode({name: 'foo', value: {name: 'bar'}});
    expect({name: 'foo', value: {name: 'bar'}})
      .not.toMatchNode({name: 'foo', value: {name: 'baz'}});

    expect({name: 'foo', value: 'bar'}).toMatchNode({name: 'foo'});
    expect({name: 'foo'}).not.toMatchNode({name: 'foo', value: 'bar'});

    expect(Object.create({name: 'foo'})).not.toMatchNode({name: 'foo'});
    expect({}).toMatchNode(Object.create({name: 'foo'}));
  })",59,74,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 61, 'column': 4, 'index': 1689}","it('matches objects', function() {
    expect({}).toMatchNode({});
    expect({name: 'foo'}).toMatchNode({name: 'foo'});
    expect({name: 'foo'}).not.toMatchNode({name: 'bar'});

    expect({name: 'foo', value: {name: 'bar'}})
      .toMatchNode({name: 'foo', value: {name: 'bar'}});
    expect({name: 'foo', value: {name: 'bar'}})
      .not.toMatchNode({name: 'foo', value: {name: 'baz'}});

    expect({name: 'foo', value: 'bar'}).toMatchNode({name: 'foo'});
    expect({name: 'foo'}).not.toMatchNode({name: 'foo', value: 'bar'});

    expect(Object.create({name: 'foo'})).not.toMatchNode({name: 'foo'});
    expect({}).toMatchNode(Object.create({name: 'foo'}));
  })",59,74,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 62, 'column': 4, 'index': 1743}","it('matches objects', function() {
    expect({}).toMatchNode({});
    expect({name: 'foo'}).toMatchNode({name: 'foo'});
    expect({name: 'foo'}).not.toMatchNode({name: 'bar'});

    expect({name: 'foo', value: {name: 'bar'}})
      .toMatchNode({name: 'foo', value: {name: 'bar'}});
    expect({name: 'foo', value: {name: 'bar'}})
      .not.toMatchNode({name: 'foo', value: {name: 'baz'}});

    expect({name: 'foo', value: 'bar'}).toMatchNode({name: 'foo'});
    expect({name: 'foo'}).not.toMatchNode({name: 'foo', value: 'bar'});

    expect(Object.create({name: 'foo'})).not.toMatchNode({name: 'foo'});
    expect({}).toMatchNode(Object.create({name: 'foo'}));
  })",59,74,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 64, 'column': 4, 'index': 1802}","it('matches objects', function() {
    expect({}).toMatchNode({});
    expect({name: 'foo'}).toMatchNode({name: 'foo'});
    expect({name: 'foo'}).not.toMatchNode({name: 'bar'});

    expect({name: 'foo', value: {name: 'bar'}})
      .toMatchNode({name: 'foo', value: {name: 'bar'}});
    expect({name: 'foo', value: {name: 'bar'}})
      .not.toMatchNode({name: 'foo', value: {name: 'baz'}});

    expect({name: 'foo', value: 'bar'}).toMatchNode({name: 'foo'});
    expect({name: 'foo'}).not.toMatchNode({name: 'foo', value: 'bar'});

    expect(Object.create({name: 'foo'})).not.toMatchNode({name: 'foo'});
    expect({}).toMatchNode(Object.create({name: 'foo'}));
  })",59,74,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 66, 'column': 4, 'index': 1907}","it('matches objects', function() {
    expect({}).toMatchNode({});
    expect({name: 'foo'}).toMatchNode({name: 'foo'});
    expect({name: 'foo'}).not.toMatchNode({name: 'bar'});

    expect({name: 'foo', value: {name: 'bar'}})
      .toMatchNode({name: 'foo', value: {name: 'bar'}});
    expect({name: 'foo', value: {name: 'bar'}})
      .not.toMatchNode({name: 'foo', value: {name: 'baz'}});

    expect({name: 'foo', value: 'bar'}).toMatchNode({name: 'foo'});
    expect({name: 'foo'}).not.toMatchNode({name: 'foo', value: 'bar'});

    expect(Object.create({name: 'foo'})).not.toMatchNode({name: 'foo'});
    expect({}).toMatchNode(Object.create({name: 'foo'}));
  })",59,74,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 70, 'column': 4, 'index': 2085}","it('matches objects', function() {
    expect({}).toMatchNode({});
    expect({name: 'foo'}).toMatchNode({name: 'foo'});
    expect({name: 'foo'}).not.toMatchNode({name: 'bar'});

    expect({name: 'foo', value: {name: 'bar'}})
      .toMatchNode({name: 'foo', value: {name: 'bar'}});
    expect({name: 'foo', value: {name: 'bar'}})
      .not.toMatchNode({name: 'foo', value: {name: 'baz'}});

    expect({name: 'foo', value: 'bar'}).toMatchNode({name: 'foo'});
    expect({name: 'foo'}).not.toMatchNode({name: 'foo', value: 'bar'});

    expect(Object.create({name: 'foo'})).not.toMatchNode({name: 'foo'});
    expect({}).toMatchNode(Object.create({name: 'foo'}));
  })",59,74,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 73, 'column': 4, 'index': 2231}","it('matches objects', function() {
    expect({}).toMatchNode({});
    expect({name: 'foo'}).toMatchNode({name: 'foo'});
    expect({name: 'foo'}).not.toMatchNode({name: 'bar'});

    expect({name: 'foo', value: {name: 'bar'}})
      .toMatchNode({name: 'foo', value: {name: 'bar'}});
    expect({name: 'foo', value: {name: 'bar'}})
      .not.toMatchNode({name: 'foo', value: {name: 'baz'}});

    expect({name: 'foo', value: 'bar'}).toMatchNode({name: 'foo'});
    expect({name: 'foo'}).not.toMatchNode({name: 'foo', value: 'bar'});

    expect(Object.create({name: 'foo'})).not.toMatchNode({name: 'foo'});
    expect({}).toMatchNode(Object.create({name: 'foo'}));
  })",59,74,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 81, 'column': 4, 'index': 2442}","it('matches with a function', function() {
    const haystack = {name: 'foo'};
    const needle = jest.fn();

    needle.mockReturnValue(true);
    expect(haystack).toMatchNode(needle);
    expect(needle).toBeCalledWith(haystack);

    needle.mockReturnValue(false);
    expect(haystack).not.toMatchNode(needle);
  })",76,86,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 85, 'column': 4, 'index': 2565}","it('matches with a function', function() {
    const haystack = {name: 'foo'};
    const needle = jest.fn();

    needle.mockReturnValue(true);
    expect(haystack).toMatchNode(needle);
    expect(needle).toBeCalledWith(haystack);

    needle.mockReturnValue(false);
    expect(haystack).not.toMatchNode(needle);
  })",76,86,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 97, 'column': 4, 'index': 2881}","it('matches nested value with a function', function() {
    const haystack = {name: 'foo', value: 'bar'};
    const needle = {
      name: jest.fn(),
      value: jest.fn(),
    };

    needle.name.mockReturnValue(true);
    needle.value.mockReturnValue(true);
    expect(haystack).toMatchNode(needle);
    expect(needle.name).toBeCalledWith(haystack.name);
    expect(needle.value).toBeCalledWith(haystack.value);

    needle.name.mockReturnValue(false);
    needle.value.mockReturnValue(true);
    expect(haystack).not.toMatchNode(needle);

    needle.name.mockReturnValue(true);
    needle.value.mockReturnValue(false);
    expect(haystack).not.toMatchNode(needle);
  })",88,108,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 103, 'column': 4, 'index': 3116}","it('matches nested value with a function', function() {
    const haystack = {name: 'foo', value: 'bar'};
    const needle = {
      name: jest.fn(),
      value: jest.fn(),
    };

    needle.name.mockReturnValue(true);
    needle.value.mockReturnValue(true);
    expect(haystack).toMatchNode(needle);
    expect(needle.name).toBeCalledWith(haystack.name);
    expect(needle.value).toBeCalledWith(haystack.value);

    needle.name.mockReturnValue(false);
    needle.value.mockReturnValue(true);
    expect(haystack).not.toMatchNode(needle);

    needle.name.mockReturnValue(true);
    needle.value.mockReturnValue(false);
    expect(haystack).not.toMatchNode(needle);
  })",88,108,steel
/src/__tests__/matchNode-test.js,Duplicate Assert,"{'line': 107, 'column': 4, 'index': 3243}","it('matches nested value with a function', function() {
    const haystack = {name: 'foo', value: 'bar'};
    const needle = {
      name: jest.fn(),
      value: jest.fn(),
    };

    needle.name.mockReturnValue(true);
    needle.value.mockReturnValue(true);
    expect(haystack).toMatchNode(needle);
    expect(needle.name).toBeCalledWith(haystack.name);
    expect(needle.value).toBeCalledWith(haystack.value);

    needle.name.mockReturnValue(false);
    needle.value.mockReturnValue(true);
    expect(haystack).not.toMatchNode(needle);

    needle.name.mockReturnValue(true);
    needle.value.mockReturnValue(false);
    expect(haystack).not.toMatchNode(needle);
  })",88,108,steel
/src/__tests__/matchNode-test.js,Magic Number,"{'line': 38, 'column': 11, 'index': 949}","it('matches scalars', function() {
    expect('foo').toMatchNode('foo');
    expect('foo').not.toMatchNode('bar');
    expect('123').not.toMatchNode(123);

    expect(123).toMatchNode(123);
    expect(123).not.toMatchNode(456);
    expect(123).not.toMatchNode('123');

    expect(true).toMatchNode(true);
    expect(true).not.toMatchNode(false);
    expect(true).not.toMatchNode('true');
  })",33,45,steel
/src/__tests__/matchNode-test.js,Magic Number,"{'line': 39, 'column': 11, 'index': 983}","it('matches scalars', function() {
    expect('foo').toMatchNode('foo');
    expect('foo').not.toMatchNode('bar');
    expect('123').not.toMatchNode(123);

    expect(123).toMatchNode(123);
    expect(123).not.toMatchNode(456);
    expect(123).not.toMatchNode('123');

    expect(true).toMatchNode(true);
    expect(true).not.toMatchNode(false);
    expect(true).not.toMatchNode('true');
  })",33,45,steel
/src/__tests__/matchNode-test.js,Magic Number,"{'line': 40, 'column': 11, 'index': 1021}","it('matches scalars', function() {
    expect('foo').toMatchNode('foo');
    expect('foo').not.toMatchNode('bar');
    expect('123').not.toMatchNode(123);

    expect(123).toMatchNode(123);
    expect(123).not.toMatchNode(456);
    expect(123).not.toMatchNode('123');

    expect(true).toMatchNode(true);
    expect(true).not.toMatchNode(false);
    expect(true).not.toMatchNode('true');
  })",33,45,steel
/src/__tests__/matchNode-test.js,Redundant Assertion,"{'column': 4, 'line': 34}","it('matches scalars', function() {
    expect('foo').toMatchNode('foo');
    expect('foo').not.toMatchNode('bar');
    expect('123').not.toMatchNode(123);

    expect(123).toMatchNode(123);
    expect(123).not.toMatchNode(456);
    expect(123).not.toMatchNode('123');

    expect(true).toMatchNode(true);
    expect(true).not.toMatchNode(false);
    expect(true).not.toMatchNode('true');
  })",33,45,steel
/src/__tests__/matchNode-test.js,Redundant Assertion,"{'column': 4, 'line': 35}","it('matches scalars', function() {
    expect('foo').toMatchNode('foo');
    expect('foo').not.toMatchNode('bar');
    expect('123').not.toMatchNode(123);

    expect(123).toMatchNode(123);
    expect(123).not.toMatchNode(456);
    expect(123).not.toMatchNode('123');

    expect(true).toMatchNode(true);
    expect(true).not.toMatchNode(false);
    expect(true).not.toMatchNode('true');
  })",33,45,steel
/src/__tests__/matchNode-test.js,Redundant Assertion,"{'column': 4, 'line': 36}","it('matches scalars', function() {
    expect('foo').toMatchNode('foo');
    expect('foo').not.toMatchNode('bar');
    expect('123').not.toMatchNode(123);

    expect(123).toMatchNode(123);
    expect(123).not.toMatchNode(456);
    expect(123).not.toMatchNode('123');

    expect(true).toMatchNode(true);
    expect(true).not.toMatchNode(false);
    expect(true).not.toMatchNode('true');
  })",33,45,steel
/src/__tests__/matchNode-test.js,Redundant Assertion,"{'column': 4, 'line': 38}","it('matches scalars', function() {
    expect('foo').toMatchNode('foo');
    expect('foo').not.toMatchNode('bar');
    expect('123').not.toMatchNode(123);

    expect(123).toMatchNode(123);
    expect(123).not.toMatchNode(456);
    expect(123).not.toMatchNode('123');

    expect(true).toMatchNode(true);
    expect(true).not.toMatchNode(false);
    expect(true).not.toMatchNode('true');
  })",33,45,steel
/src/__tests__/matchNode-test.js,Redundant Assertion,"{'column': 4, 'line': 39}","it('matches scalars', function() {
    expect('foo').toMatchNode('foo');
    expect('foo').not.toMatchNode('bar');
    expect('123').not.toMatchNode(123);

    expect(123).toMatchNode(123);
    expect(123).not.toMatchNode(456);
    expect(123).not.toMatchNode('123');

    expect(true).toMatchNode(true);
    expect(true).not.toMatchNode(false);
    expect(true).not.toMatchNode('true');
  })",33,45,steel
/src/__tests__/matchNode-test.js,Redundant Assertion,"{'column': 4, 'line': 40}","it('matches scalars', function() {
    expect('foo').toMatchNode('foo');
    expect('foo').not.toMatchNode('bar');
    expect('123').not.toMatchNode(123);

    expect(123).toMatchNode(123);
    expect(123).not.toMatchNode(456);
    expect(123).not.toMatchNode('123');

    expect(true).toMatchNode(true);
    expect(true).not.toMatchNode(false);
    expect(true).not.toMatchNode('true');
  })",33,45,steel
/src/__tests__/matchNode-test.js,Redundant Assertion,"{'column': 4, 'line': 42}","it('matches scalars', function() {
    expect('foo').toMatchNode('foo');
    expect('foo').not.toMatchNode('bar');
    expect('123').not.toMatchNode(123);

    expect(123).toMatchNode(123);
    expect(123).not.toMatchNode(456);
    expect(123).not.toMatchNode('123');

    expect(true).toMatchNode(true);
    expect(true).not.toMatchNode(false);
    expect(true).not.toMatchNode('true');
  })",33,45,steel
/src/__tests__/matchNode-test.js,Redundant Assertion,"{'column': 4, 'line': 43}","it('matches scalars', function() {
    expect('foo').toMatchNode('foo');
    expect('foo').not.toMatchNode('bar');
    expect('123').not.toMatchNode(123);

    expect(123).toMatchNode(123);
    expect(123).not.toMatchNode(456);
    expect(123).not.toMatchNode('123');

    expect(true).toMatchNode(true);
    expect(true).not.toMatchNode(false);
    expect(true).not.toMatchNode('true');
  })",33,45,steel
/src/__tests__/matchNode-test.js,Redundant Assertion,"{'column': 4, 'line': 44}","it('matches scalars', function() {
    expect('foo').toMatchNode('foo');
    expect('foo').not.toMatchNode('bar');
    expect('123').not.toMatchNode(123);

    expect(123).toMatchNode(123);
    expect(123).not.toMatchNode(456);
    expect(123).not.toMatchNode('123');

    expect(true).toMatchNode(true);
    expect(true).not.toMatchNode(false);
    expect(true).not.toMatchNode('true');
  })",33,45,steel
/src/__tests__/core-test.js,Magic Number,"{'line': 74, 'column': 20, 'index': 2038}","it('plugins are only registered once', function () {
    let ct = 0;

    function plugin() {
      ct++;
    }

    core.use(plugin);
    core.use(plugin);

    expect(ct).toBe(1);
  })",64,75,steel
/src/__tests__/argsParser-test.js,Conditional Test Logic,"{'line': 60, 'column': 6, 'index': 1501}","describe('options', function() {
    function test(testCases) {
      for (const testName in testCases) {
        const testCase = testCases[testName];

        const parser = argsParser.options(testCase.options);

        it(testName + ' (space separated values)', function() {
          const parse = () => parser.parse(
            Array.prototype.concat.apply([], testCase.args)
          );

          if (typeof testCase.expected === 'string') {
            expect(parse).toThrowError(testCase.expected);
          } else {
            expect(parse()).toEqual(testCase.expected);
          }
        });

        it(testName + ' (= separated values)', function() {
          const parse = () => parser.parse(
            testCase.args.map(args => args.join('='))
          );
          if (typeof testCase.expected === 'string') {
            expect(parse).toThrowError(testCase.expected);
          } else {
            expect(parse()).toEqual(testCase.expected);
          }
        });

      }
    }

    test({
      'understands separate arg name and short option names': {
        options: {
          foo: {
            full: 'another-foo',
          },
          bar: {
            abbr: 'b',
          },
        },
        args: [['--another-foo', 'oof'], ['-b', 'rab']],
        expected: {
          options: {
            foo: 'oof',
            bar: 'rab',
          },
          positionalArguments: []
        },
      },

      'understands default values': {
        options: {
          foo: {},
          bar: {
            default: 'rab',
          },
          baz: {
            abbr: 'b',
            default: 'zab',
          },
        },
        args: [['--foo', 'oof']],
        expected: {
          options: {
            foo: 'oof',
            bar: 'rab',
            baz: 'zab',
          },
          positionalArguments: []
        },
      },

      'allows preprocessing values': {
        options: {
          foo: {},
          bar: {
            default: 456,
          },
          bay: {
            process: Number,
          },
          baz: {
            abbr: 'b',
            process: v => v+v,
          },
        },
        args: [['--foo', '123'], ['--bay', '789'], ['-b', 'zab']],
        expected: {
          options: {
            foo: '123',
            bar: 456,
            bay: 789,
            baz: 'zabzab',
          },
          positionalArguments: []
        },
      },

      'understands lists': {
        options: {
          foo: {
            list: true,
          },
          bar: {
            list: true,
          },
          baz: {},
        },
        args: [
          ['--foo', 'oof1'],
          ['--baz', 'zab1'],
          ['--foo', 'oof2'],
          ['--baz', 'zab2'],
        ],
        expected: {
          options: {
            foo: ['oof1', 'oof2'],
            bar: [],
            baz: 'zab2',
          },
          positionalArguments: []
        },
      },

      'errors when an option does not have a value (1)': {
        options: {
          foo: {},
          bar: {
            abbr: 'b',
          },
          baz: {},
        },
        args: [
          ['--foo', 'oof'],
          ['-b'],
          ['--baz', 'zab'],
        ],
        expected: '--bar requires a value',
      },

      'errors when an option does not have a value (2)': {
        options: {
          foo: {},
          bar: {
            abbr: 'b',
          },
          baz: {},
        },
        args: [
          ['--foo', 'oof'],
          ['--bar'],
          ['--baz', 'zab'],
        ],
        expected: '--bar requires a value',
      },

      'errors when an option does not have a value (3)': {
        options: {
          foo: {
            default: 'oof',
          },
        },
        args: [['--foo']],
        expected: '--foo requires a value',
      },

      'understands choices': {
        options: {
          foo: {
            choices: ['oof'],
          },
        },
        args: [
          ['--foo', 'oof'],
        ],
        expected: {
          options: {
            foo: 'oof',
          },
          positionalArguments: []
        },
      },

      'errors if choice does not match': {
        options: {
          foo: {
            choices: ['oof'],
          },
          bar: {
            choices: ['rab1', 'rab2'],
          },
        },
        args: [
          ['--foo', 'oof'],
          ['--bar', 'rab'],
        ],
        expected: '--bar must be one of the values rab1,rab2',
      },

      'accepts unkown options': {
        options: {},
        args: [
          ['--foo'],
          ['--bar'],
          ['--bay', 'yab'],
          ['foo'],
          ['--b', 'zab1'],
          ['--foo', 'oof'],
          ['--b', 'zab2'],
          ['bar'],
        ],
        expected: {
          options: {
            foo: 'oof',
            bar: true,
            bay: 'yab',
            b: ['zab1', 'zab2'],
          },
          positionalArguments: ['foo', 'bar'],
        },
      },

      'parses unkown options as JSON': {
        options: {},
        args: [
          ['--foo', '{""foo"": ""bar""}'],
        ],
        expected: {
          options: {
            foo: {foo: 'bar'},
          },
          positionalArguments: [],
        },
      },
    });
  })",58,296,steel
/src/__tests__/argsParser-test.js,Conditional Test Logic,"{'line': 70, 'column': 10, 'index': 1838}","it(testName + ' (space separated values)', function() {
          const parse = () => parser.parse(
            Array.prototype.concat.apply([], testCase.args)
          );

          if (typeof testCase.expected === 'string') {
            expect(parse).toThrowError(testCase.expected);
          } else {
            expect(parse()).toEqual(testCase.expected);
          }
        })",65,75,steel
/src/__tests__/argsParser-test.js,Conditional Test Logic,"{'line': 81, 'column': 10, 'index': 2223}","it(testName + ' (= separated values)', function() {
          const parse = () => parser.parse(
            testCase.args.map(args => args.join('='))
          );
          if (typeof testCase.expected === 'string') {
            expect(parse).toThrowError(testCase.expected);
          } else {
            expect(parse()).toEqual(testCase.expected);
          }
        })",77,86,steel
/src/__tests__/argsParser-test.js,Exception Handling,"{'line': 18, 'column': 4, 'index': 446}","it('prints the help text', function() {
    const parser = argsParser.options({});
    let exception;
    try {
      parser.parse(['--help']);
    } catch(e) {
      exception = e;
    }
    expect(exception.exitCode).toEqual(0);
    expect(exception.message).toEqual(parser.getHelpText());
  })",15,25,steel
/src/__tests__/argsParser-test.js,Magic Number,"{'line': 23, 'column': 39, 'index': 567}","it('prints the help text', function() {
    const parser = argsParser.options({});
    let exception;
    try {
      parser.parse(['--help']);
    } catch(e) {
      exception = e;
    }
    expect(exception.exitCode).toEqual(0);
    expect(exception.message).toEqual(parser.getHelpText());
  })",15,25,steel
/src/__tests__/Worker-test.js,Conditional Test Logic,"{'line': 89, 'column': 24, 'index': 2678}","describe('custom parser', () => {
    function getTransformForParser(parser) {
      return createTempFileWith(
        `function transform(fileInfo, api) {
          api.jscodeshift(fileInfo.source);
          return ""changed"";
         }
         ${parser ? `transform.parser = '${parser}';` : ''}
         module.exports = transform;
        `
      );
    }
    function getSourceFile() {
      // This code cannot be parsed by Babel v5
      return createTempFileWith(
         'const x = (a: Object, b: string): void => {}'
      );
    }

    it('errors if new flow type code is parsed with babel v5', done => {
      const worker = require('../Worker');
      const transformPath = createTransformWith(
        'api.jscodeshift(fileInfo.source); return ""changed"";'
      );
      const sourcePath = getSourceFile();
      const emitter = worker([transformPath]);

      emitter.send({files: [sourcePath]});
      emitter.once('message', (data) => {
        expect(data.status).toBe('error');
        expect(data.msg).toMatch('SyntaxError');
        done();
      });
    });

    ['flow', 'babylon'].forEach(parser => {
      it(`uses ${parser} if configured as such`, done => {
        const worker = require('../Worker');
        const transformPath = getTransformForParser(parser);
        const sourcePath = getSourceFile();
        const emitter = worker([transformPath]);

        emitter.send({files: [sourcePath]});
        emitter.once('message', (data) => {
          expect(data.status).toBe('ok');
          expect(getFileContent(sourcePath)).toBe('changed');
          done();
        });
      });
    });

    ['babylon', 'flow', 'tsx'].forEach(parser => {
      it(`can parse JSX with ${parser}`, done => {
        const worker = require('../Worker');
        const transformPath = getTransformForParser(parser);
        const sourcePath = createTempFileWith(
          'var component = <div>{foobar}</div>;'
        );
        const emitter = worker([transformPath]);

        emitter.send({files: [sourcePath]});
        emitter.once('message', (data) => {
          expect(data.status).toBe('ok');
          expect(getFileContent(sourcePath)).toBe('changed');
          done();
        });
      });
    });

    it('can parse enums with flow', done => {
      const worker = require('../Worker');
      const transformPath = getTransformForParser('flow');
      const sourcePath = createTempFileWith(
        'enum E {A, B}'
      );
      const emitter = worker([transformPath]);

      emitter.send({files: [sourcePath]});
      emitter.once('message', (data) => {
        expect(data.status).toBe('ok');
        expect(getFileContent(sourcePath)).toBe('changed');
        done();
      });
    });
  })",54,138,steel
/src/__tests__/Worker-test.js,Conditional Test Logic,"{'line': 105, 'column': 31, 'index': 3230}","describe('custom parser', () => {
    function getTransformForParser(parser) {
      return createTempFileWith(
        `function transform(fileInfo, api) {
          api.jscodeshift(fileInfo.source);
          return ""changed"";
         }
         ${parser ? `transform.parser = '${parser}';` : ''}
         module.exports = transform;
        `
      );
    }
    function getSourceFile() {
      // This code cannot be parsed by Babel v5
      return createTempFileWith(
         'const x = (a: Object, b: string): void => {}'
      );
    }

    it('errors if new flow type code is parsed with babel v5', done => {
      const worker = require('../Worker');
      const transformPath = createTransformWith(
        'api.jscodeshift(fileInfo.source); return ""changed"";'
      );
      const sourcePath = getSourceFile();
      const emitter = worker([transformPath]);

      emitter.send({files: [sourcePath]});
      emitter.once('message', (data) => {
        expect(data.status).toBe('error');
        expect(data.msg).toMatch('SyntaxError');
        done();
      });
    });

    ['flow', 'babylon'].forEach(parser => {
      it(`uses ${parser} if configured as such`, done => {
        const worker = require('../Worker');
        const transformPath = getTransformForParser(parser);
        const sourcePath = getSourceFile();
        const emitter = worker([transformPath]);

        emitter.send({files: [sourcePath]});
        emitter.once('message', (data) => {
          expect(data.status).toBe('ok');
          expect(getFileContent(sourcePath)).toBe('changed');
          done();
        });
      });
    });

    ['babylon', 'flow', 'tsx'].forEach(parser => {
      it(`can parse JSX with ${parser}`, done => {
        const worker = require('../Worker');
        const transformPath = getTransformForParser(parser);
        const sourcePath = createTempFileWith(
          'var component = <div>{foobar}</div>;'
        );
        const emitter = worker([transformPath]);

        emitter.send({files: [sourcePath]});
        emitter.once('message', (data) => {
          expect(data.status).toBe('ok');
          expect(getFileContent(sourcePath)).toBe('changed');
          done();
        });
      });
    });

    it('can parse enums with flow', done => {
      const worker = require('../Worker');
      const transformPath = getTransformForParser('flow');
      const sourcePath = createTempFileWith(
        'enum E {A, B}'
      );
      const emitter = worker([transformPath]);

      emitter.send({files: [sourcePath]});
      emitter.once('message', (data) => {
        expect(data.status).toBe('ok');
        expect(getFileContent(sourcePath)).toBe('changed');
        done();
      });
    });
  })",54,138,steel
/src/__tests__/Collection-test.js,Conditional Test Logic,"{'line': 180, 'column': 8, 'index': 5586}","describe('hasConflictingRegistration', function () {
      function register(methodName, type) {
        const methods = {};
        methods[methodName] = function () {};
        if (!types[type]) {
          throw new Error(type + ' is not a valid type');
        }
        Collection.registerMethods(methods, types[type]);
      }

      it('true if supertype is registered', function () {
        register('supertypeIsRegistered', 'Expression');
        expect(Collection.hasConflictingRegistration('supertypeIsRegistered', 'FunctionExpression')).toBe(true);
      });

      it('true if subtype is registered', function () {
        register('subtypeIsRegistered', 'FunctionExpression');
        expect(Collection.hasConflictingRegistration('subtypeIsRegistered', 'Expression')).toBe(true);
      });

      it('false if only a sibling type is registered', function () {
        register('siblingIsRegistered', 'FunctionExpression');
        expect(Collection.hasConflictingRegistration('siblingIsRegistered', 'BinaryExpression')).toBe(false);
      });
    })",176,200,steel
/src/__tests__/Collection-test.js,Duplicate Assert,"{'line': 67, 'column': 6, 'index': 1798}","it('returns a collection of the closest common type', function() {
      let nodes = [
        b.identifier('foo'),
        b.sequenceExpression([]),
      ];
      expect(Collection.fromNodes(nodes).getTypes())
        .toContain('Expression');

      nodes = [
        b.identifier('foo'),
        b.blockStatement([]),
      ];
      expect(Collection.fromNodes(nodes).getTypes())
        .toContain('Node');
    })",62,76,steel
/src/__tests__/Collection-test.js,Duplicate Assert,"{'line': 74, 'column': 6, 'index': 1970}","it('returns a collection of the closest common type', function() {
      let nodes = [
        b.identifier('foo'),
        b.sequenceExpression([]),
      ];
      expect(Collection.fromNodes(nodes).getTypes())
        .toContain('Expression');

      nodes = [
        b.identifier('foo'),
        b.blockStatement([]),
      ];
      expect(Collection.fromNodes(nodes).getTypes())
        .toContain('Node');
    })",62,76,steel
/src/__tests__/Collection-test.js,Magic Number,"{'line': 211, 'column': 46, 'index': 6847}","it('lets you filter with custom logic', function() {
        const filter = jest.fn(function(path) {
          return path.value.name === 'foo';
        });
        const fooVariables = Collection.fromNodes(nodes).filter(filter);

        expect(filter.mock.calls.length).toBe(2);
        expect(fooVariables.length).toBe(1);
      })",205,213,steel
/src/__tests__/Collection-test.js,Magic Number,"{'line': 212, 'column': 41, 'index': 6892}","it('lets you filter with custom logic', function() {
        const filter = jest.fn(function(path) {
          return path.value.name === 'foo';
        });
        const fooVariables = Collection.fromNodes(nodes).filter(filter);

        expect(filter.mock.calls.length).toBe(2);
        expect(fooVariables.length).toBe(1);
      })",205,213,steel
/src/__tests__/Collection-test.js,Magic Number,"{'line': 222, 'column': 44, 'index': 7158}","it('lets you iterate over each element of an collection', function() {
        const each = jest.fn();
        Collection.fromNodes(nodes).forEach(each);

        expect(each.mock.calls.length).toBe(2);
        expect(each.mock.calls[0][0].value).toBe(nodes[0]);
        expect(each.mock.calls[1][0].value).toBe(nodes[1]);
      })",218,225,steel
/src/__tests__/Collection-test.js,Magic Number,"{'line': 240, 'column': 44, 'index': 7815}","it('lets you test each element of a collection and stops when one passes the test', function() {
        const each = jest.fn().mockImplementation(() => true);
        Collection.fromNodes(nodes).some(each);

        expect(each.mock.calls.length).toBe(1);
        expect(each.mock.calls[0][0].value).toBe(nodes[0]);
      })",236,242,steel
/src/__tests__/Collection-test.js,Magic Number,"{'line': 260, 'column': 44, 'index': 8578}","it('lets you test each element of a collection and stops when one fails the test', function() {
        const each = jest.fn().mockImplementation(() => false);
        Collection.fromNodes(nodes).every(each);

        expect(each.mock.calls.length).toBe(1);
        expect(each.mock.calls[0][0].value).toBe(nodes[0]);
      })",256,262,steel
/src/__tests__/Collection-test.js,Magic Number,"{'line': 281, 'column': 35, 'index': 9351}","it('returns a new collection with mapped values', function() {
        const root = Collection.fromNodes(nodes);
        const mapped = root.map((_, i) => new NodePath(nodes[+!i]));

        expect(root).not.toBe(mapped);
        expect(mapped.length).toBe(2);
        expect(mapped.nodes()[0]).toBe(nodes[1]);
        expect(mapped.nodes()[1]).toBe(nodes[0]);
      })",276,284,steel
/src/__tests__/Collection-test.js,Magic Number,"{'line': 292, 'column': 35, 'index': 9723}","it('dedupes elements', function() {
        const path = new NodePath(nodes[0]);
        const root = Collection.fromNodes(nodes);
        const mapped = root.map(() => path);

        expect(root).not.toBe(mapped);
        expect(mapped.length).toBe(1);
        expect(mapped.paths()[0]).toBe(path);
      })",286,294,steel
/bin/__tests__/jscodeshift-test.js,Conditional Test Logic,"{'line': 43, 'column': 4, 'index': 1185}",Unknown,,,steel
/bin/__tests__/jscodeshift-test.js,Conditional Test Logic,"{'line': 241, 'column': 4, 'index': 7329}","it('does not stall with too many files', () => {
    const sources = [];
    for (let i = 0; i < 100; i++) {
      sources.push(createTempFileWith('a'));
    }
    const transform = createTransformWith('');
    return run(['-t', transform, '--foo=42'].concat(sources)).then(
      () => {
        expect(true).toBe(true);
      }
    );
  })",239,250,steel
/bin/__tests__/jscodeshift-test.js,Duplicate Assert,"{'line': 66, 'column': 10, 'index': 1850}","it('calls the transform and file information', () => {
    const sourceA = createTempFileWith('a', 'sourceA', '.js');
    const sourceB = createTempFileWith('b\n', 'sourceB', '.js');
    const sourceC = createTempFileWith('c', 'sourceC', '.js');
    const transformA = createTransformWith(
      'return ""transform"" + fileInfo.source;'
    );
    const transformB = createTransformWith(
      'return fileInfo.path;'
    );

    return Promise.all([
      run(['-t', transformA, sourceA, sourceB]).then(
        out => {
          expect(out[1]).toBe('');
          expect(readFile(sourceA)).toBe('transforma');
          expect(readFile(sourceB)).toBe('transformb\n');
        }
      ),
      run(['-t', transformB, sourceC]).then(
        out => {
          expect(out[1]).toBe('');
          expect(readFile(sourceC)).toBe(sourceC);
        }
      )
    ]);
  })",52,78,steel
/bin/__tests__/jscodeshift-test.js,Duplicate Assert,"{'line': 66, 'column': 10, 'index': 1850}","it('calls the transform and file information', () => {
    const sourceA = createTempFileWith('a', 'sourceA', '.js');
    const sourceB = createTempFileWith('b\n', 'sourceB', '.js');
    const sourceC = createTempFileWith('c', 'sourceC', '.js');
    const transformA = createTransformWith(
      'return ""transform"" + fileInfo.source;'
    );
    const transformB = createTransformWith(
      'return fileInfo.path;'
    );

    return Promise.all([
      run(['-t', transformA, sourceA, sourceB]).then(
        out => {
          expect(out[1]).toBe('');
          expect(readFile(sourceA)).toBe('transforma');
          expect(readFile(sourceB)).toBe('transformb\n');
        }
      ),
      run(['-t', transformB, sourceC]).then(
        out => {
          expect(out[1]).toBe('');
          expect(readFile(sourceC)).toBe(sourceC);
        }
      )
    ]);
  })",52,78,steel
/bin/__tests__/jscodeshift-test.js,Duplicate Assert,"{'line': 73, 'column': 10, 'index': 2080}","it('calls the transform and file information', () => {
    const sourceA = createTempFileWith('a', 'sourceA', '.js');
    const sourceB = createTempFileWith('b\n', 'sourceB', '.js');
    const sourceC = createTempFileWith('c', 'sourceC', '.js');
    const transformA = createTransformWith(
      'return ""transform"" + fileInfo.source;'
    );
    const transformB = createTransformWith(
      'return fileInfo.path;'
    );

    return Promise.all([
      run(['-t', transformA, sourceA, sourceB]).then(
        out => {
          expect(out[1]).toBe('');
          expect(readFile(sourceA)).toBe('transforma');
          expect(readFile(sourceB)).toBe('transformb\n');
        }
      ),
      run(['-t', transformB, sourceC]).then(
        out => {
          expect(out[1]).toBe('');
          expect(readFile(sourceC)).toBe(sourceC);
        }
      )
    ]);
  })",52,78,steel
/bin/__tests__/jscodeshift-test.js,Duplicate Assert,"{'line': 73, 'column': 10, 'index': 2080}","it('calls the transform and file information', () => {
    const sourceA = createTempFileWith('a', 'sourceA', '.js');
    const sourceB = createTempFileWith('b\n', 'sourceB', '.js');
    const sourceC = createTempFileWith('c', 'sourceC', '.js');
    const transformA = createTransformWith(
      'return ""transform"" + fileInfo.source;'
    );
    const transformB = createTransformWith(
      'return fileInfo.path;'
    );

    return Promise.all([
      run(['-t', transformA, sourceA, sourceB]).then(
        out => {
          expect(out[1]).toBe('');
          expect(readFile(sourceA)).toBe('transforma');
          expect(readFile(sourceB)).toBe('transformb\n');
        }
      ),
      run(['-t', transformB, sourceC]).then(
        out => {
          expect(out[1]).toBe('');
          expect(readFile(sourceC)).toBe(sourceC);
        }
      )
    ]);
  })",52,78,steel
/bin/__tests__/jscodeshift-test.js,Duplicate Assert,"{'line': 375, 'column': 10, 'index': 12478}","it('shows workers info and stats at the end by default', () => {
      const sourceA = createTempFileWith('a', 'sourceA', '.js');
      const transform = createTransformWith('return null;');
      return run(['-t', transform, sourceA]).then(
        out => {
          expect(out[0]).toContain('Processing 1 files...');
          expect(out[0]).toContain('Spawning 1 workers...');
          expect(out[0]).toContain('Sending 1 files to free worker...');
          expect(out[0]).toContain('All done.');
          expect(out[0]).toContain('Results: ');
          expect(out[0]).toContain('Time elapsed: ');
        }
      );
    })",370,383,steel
/bin/__tests__/jscodeshift-test.js,Duplicate Assert,"{'line': 376, 'column': 10, 'index': 12539}","it('shows workers info and stats at the end by default', () => {
      const sourceA = createTempFileWith('a', 'sourceA', '.js');
      const transform = createTransformWith('return null;');
      return run(['-t', transform, sourceA]).then(
        out => {
          expect(out[0]).toContain('Processing 1 files...');
          expect(out[0]).toContain('Spawning 1 workers...');
          expect(out[0]).toContain('Sending 1 files to free worker...');
          expect(out[0]).toContain('All done.');
          expect(out[0]).toContain('Results: ');
          expect(out[0]).toContain('Time elapsed: ');
        }
      );
    })",370,383,steel
/bin/__tests__/jscodeshift-test.js,Duplicate Assert,"{'line': 377, 'column': 10, 'index': 12600}","it('shows workers info and stats at the end by default', () => {
      const sourceA = createTempFileWith('a', 'sourceA', '.js');
      const transform = createTransformWith('return null;');
      return run(['-t', transform, sourceA]).then(
        out => {
          expect(out[0]).toContain('Processing 1 files...');
          expect(out[0]).toContain('Spawning 1 workers...');
          expect(out[0]).toContain('Sending 1 files to free worker...');
          expect(out[0]).toContain('All done.');
          expect(out[0]).toContain('Results: ');
          expect(out[0]).toContain('Time elapsed: ');
        }
      );
    })",370,383,steel
/bin/__tests__/jscodeshift-test.js,Duplicate Assert,"{'line': 378, 'column': 10, 'index': 12673}","it('shows workers info and stats at the end by default', () => {
      const sourceA = createTempFileWith('a', 'sourceA', '.js');
      const transform = createTransformWith('return null;');
      return run(['-t', transform, sourceA]).then(
        out => {
          expect(out[0]).toContain('Processing 1 files...');
          expect(out[0]).toContain('Spawning 1 workers...');
          expect(out[0]).toContain('Sending 1 files to free worker...');
          expect(out[0]).toContain('All done.');
          expect(out[0]).toContain('Results: ');
          expect(out[0]).toContain('Time elapsed: ');
        }
      );
    })",370,383,steel
/bin/__tests__/jscodeshift-test.js,Duplicate Assert,"{'line': 379, 'column': 10, 'index': 12722}","it('shows workers info and stats at the end by default', () => {
      const sourceA = createTempFileWith('a', 'sourceA', '.js');
      const transform = createTransformWith('return null;');
      return run(['-t', transform, sourceA]).then(
        out => {
          expect(out[0]).toContain('Processing 1 files...');
          expect(out[0]).toContain('Spawning 1 workers...');
          expect(out[0]).toContain('Sending 1 files to free worker...');
          expect(out[0]).toContain('All done.');
          expect(out[0]).toContain('Results: ');
          expect(out[0]).toContain('Time elapsed: ');
        }
      );
    })",370,383,steel
/bin/__tests__/jscodeshift-test.js,Duplicate Assert,"{'line': 380, 'column': 10, 'index': 12771}","it('shows workers info and stats at the end by default', () => {
      const sourceA = createTempFileWith('a', 'sourceA', '.js');
      const transform = createTransformWith('return null;');
      return run(['-t', transform, sourceA]).then(
        out => {
          expect(out[0]).toContain('Processing 1 files...');
          expect(out[0]).toContain('Spawning 1 workers...');
          expect(out[0]).toContain('Sending 1 files to free worker...');
          expect(out[0]).toContain('All done.');
          expect(out[0]).toContain('Results: ');
          expect(out[0]).toContain('Time elapsed: ');
        }
      );
    })",370,383,steel
/bin/__tests__/jscodeshift-test.js,Redundant Assertion,"{'column': 8, 'line': 247}","it('does not stall with too many files', () => {
    const sources = [];
    for (let i = 0; i < 100; i++) {
      sources.push(createTempFileWith('a'));
    }
    const transform = createTransformWith('');
    return run(['-t', transform, '--foo=42'].concat(sources)).then(
      () => {
        expect(true).toBe(true);
      }
    );
  })",239,250,steel
/bin/__tests__/jscodeshift-test.js,Redundant Print,"{'line': 346, 'column': 8, 'index': 11359}","it('sources ignore patterns from a root directory\'s .gitignore file', () => {
      // This test is a little different from the one above only in that we have
      // to simulate automatically hitting up the .gitignore file from the current
      // directory that the codeshift process is running from
      const patterns = ['sub/dir/', '*-test.js'];
      const gitignore = createTempFileWith(patterns.join('\n'), '.gitignore');
      sources.push(createTempFileWith('subfile', 'sub/dir/file.js'));

      // Make the temp directory with our test files the current working directory
      let currPath = process.cwd();
      let tempDirPath = path.dirname(sources[0]);
      process.chdir(tempDirPath);;

      return run(['-t', transform, '--gitignore'].concat(sources)).then(
        () => {
          expect(readFile(sources[0]).toString()).toBe('transforma');
          expect(readFile(sources[1]).toString()).toBe('a');
          expect(readFile(sources[2]).toString()).toBe('subfile');
        }
      )
      .catch((err) => {
        console.log(err);
      })
      .finally(() => {
        process.chdir(currPath);
      })
    })",325,351,steel
